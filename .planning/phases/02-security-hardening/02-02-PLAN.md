---
phase: 02-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/task-manager.js
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "stopTask() 在 SSH 杀进程后，追加执行 rm -f /tmp/dy_ck_${taskId}.txt 清除 Cookie 明文文件"
    - "Cookie 文件清除失败时静默忽略（不影响任务停止主流程）"
    - "rm -f 命令仅在 task.vps_id 存在时执行（与现有进程 kill 逻辑保持一致）"
    - "node -e \"require('./services/task-manager')\" 无报错"
  artifacts:
    - path: "services/task-manager.js"
      provides: "stopTask() 在任务停止时清理 /tmp/dy_ck_${taskId}.txt"
  key_links:
    - from: "stopTask()"
      to: "/tmp/dy_ck_N.txt"
      via: "SSH rm -f（与 pkill/kill 同一 exec 调用合并）"
      pattern: "dy_ck_"
---

<objective>
修复 SEC-01 的 Cookie 文件残留问题：`stopTask()` 停止任务后，VPS 上的 `/tmp/dy_ck_N.txt` 
（含抖音 Cookie 明文）没有被清除，长期积累在 VPS 的 /tmp 目录中，是明文凭证暴露风险。

将 `rm -f /tmp/dy_ck_${taskId}.txt` 追加到现有的 SSH kill 命令中，确保任务停止时 Cookie 文件被清除。

Purpose: 最小化修复——一行命令追加。独立于 crypto.js，可并行执行。
Output: 修改后的 task-manager.js，stopTask 的 SSH exec 命令包含 Cookie 文件清除。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 当前 stopTask SSH 命令（第 596-600 行）-->
```js
if (task.remote_pid && task.vps_id) {
  await sshService.exec(
    task.vps_id,
    `pkill -P ${task.remote_pid} 2>/dev/null; kill ${task.remote_pid} 2>/dev/null; true`,
    task.user_id
  ).catch(() => {});
  await new Promise(r => setTimeout(r, 700));
  await syncAutoRecordingMediaFile(task).catch(() => {});
}
```

<!-- Cookie 文件路径模式 -->
const ckFile = `/tmp/dy_ck_${taskId}.txt`;  // 第 536 行

<!-- 目标：追加 rm -f 清除 Cookie 文件 -->
```js
if (task.remote_pid && task.vps_id) {
  await sshService.exec(
    task.vps_id,
    `pkill -P ${task.remote_pid} 2>/dev/null; kill ${task.remote_pid} 2>/dev/null; rm -f /tmp/dy_ck_${taskId}.txt 2>/dev/null; true`,
    task.user_id
  ).catch(() => {});
  // ...其余不变
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 stopTask() — 追加 Cookie 文件清除命令</name>
  <files>services/task-manager.js</files>
  <action>
    在 `stopTask(taskId, userId = null)` 函数（第 590 行附近）中，
    找到 SSH exec 调用的命令字符串：
    
    ```js
    `pkill -P ${task.remote_pid} 2>/dev/null; kill ${task.remote_pid} 2>/dev/null; true`
    ```
    
    将其替换为：
    ```js
    `pkill -P ${task.remote_pid} 2>/dev/null; kill ${task.remote_pid} 2>/dev/null; rm -f /tmp/dy_ck_${taskId}.txt 2>/dev/null; true`
    ```
    
    **注意：**
    - `taskId` 在函数参数中已定义（函数签名 `async function stopTask(taskId, userId = null)`）
    - `rm -f` 使用 `2>/dev/null` 静默忽略文件不存在的错误
    - 整条命令以 `true` 结尾，确保 SSH exec 总是退出码 0
    - `.catch(() => {})` 已存在，任何 SSH 失败都会被忽略
  </action>
  <verify>node -e "const tm = require('./services/task-manager'); console.log('task-manager 加载成功'); process.exit(0)"</verify>
  <done>
    1. `grep -n "dy_ck_" services/task-manager.js` 在 stopTask 函数范围内有输出（rm -f 命令）
    2. `grep -c "dy_ck_" services/task-manager.js` 输出 >= 2（旧的写入点 + 新的删除点）
    3. `node -e "require('./services/task-manager')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证修改正确性</name>
  <files>services/task-manager.js</files>
  <action>
    执行验证命令：
    
    1. 确认 rm -f 命令已添加到 stopTask：
       ```
       grep -n "rm -f.*dy_ck" services/task-manager.js
       ```
       期望：有输出（在 stopTask 函数范围内，第 590-610 行附近）
    
    2. 确认 dy_ck 共有 2 处（写入 + 删除）：
       ```
       grep -n "dy_ck_" services/task-manager.js
       ```
       期望：至少 2 行（第 536 行写入 + 新增的 rm -f）
    
    3. 模块加载：
       ```
       node -e "const tm = require('./services/task-manager'); console.log(typeof tm.stopTask === 'function' ? 'OK' : 'FAIL'); process.exit(0)"
       ```
       期望：输出 OK
  </action>
  <verify>node -e "const tm = require('./services/task-manager'); if(typeof tm.stopTask !== 'function') throw new Error('stopTask missing'); console.log('OK'); process.exit(0)"</verify>
  <done>
    所有验证命令符合预期，模块加载成功，stopTask 是函数。
  </done>
</task>

</tasks>

<verification>
1. `grep -n "rm -f.*dy_ck" services/task-manager.js` — 有输出
2. `node -e "require('./services/task-manager')"` — 无报错
</verification>

<success_criteria>
- [ ] stopTask() 的 SSH exec 命令中包含 `rm -f /tmp/dy_ck_${taskId}.txt`
- [ ] 清除命令使用 `2>/dev/null` 忽略文件不存在的错误
- [ ] 整条命令仍以 `true` 结尾，保持幂等性
- [ ] 模块加载无报错，stopTask 仍可被调用
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening/02-02-SUMMARY.md` with:
- 修改的行号和变更内容
- 新的 SSH exec 命令字符串（完整）
</output>
