---
phase: 02-security-hardening
plan: 03
type: execute
wave: 2
depends_on: [02-01-PLAN.md]
files_modified:
  - routes/vps.js
  - services/ssh.js
  - routes/settings.js
  - services/task-manager.js
  - scripts/migrate-encrypt.js
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "routes/vps.js 在 INSERT INTO vps 前对 password 和 private_key 调用 encrypt()"
    - "services/ssh.js 的 buildConfig() 在使用 vps.password 和 vps.private_key 前调用 decrypt()"
    - "routes/settings.js 在 setSetting douyin_cookies 前调用 encrypt()，在 getCfg 读取 douyin_cookies 后调用 decrypt()"
    - "services/task-manager.js 第 201 行 getSetting('douyin_cookies') 后调用 decrypt()"
    - "scripts/migrate-encrypt.js 存在，可迁移现有明文数据"
    - "node -e \"require('./services/ssh')\" 无报错"
    - "node -e \"require('./routes/vps')\" 无报错"
  artifacts:
    - path: "routes/vps.js"
      provides: "VPS 凭证写入前加密"
    - path: "services/ssh.js"
      provides: "SSH 连接前解密凭证"
    - path: "routes/settings.js"
      provides: "Douyin Cookie 写入前加密、读取后解密"
    - path: "services/task-manager.js"
      provides: "getCookies() 对 douyin_cookies 解密"
    - path: "scripts/migrate-encrypt.js"
      provides: "一次性迁移脚本：将现有明文凭证加密存储"
  key_links:
    - from: "routes/vps.js POST /"
      to: "db vps.password / vps.private_key"
      via: "encrypt() 写入前加密"
      pattern: "encrypt"
    - from: "services/ssh.js buildConfig()"
      to: "vps.private_key / vps.password"
      via: "decrypt() 使用前解密"
      pattern: "decrypt"
    - from: "routes/settings.js setSetting douyin_cookies"
      to: "settings.douyin_cookies"
      via: "encrypt() 写入前加密"
      pattern: "encrypt.*douyin"
    - from: "services/task-manager.js getCookies()"
      to: "settings.douyin_cookies"
      via: "decrypt() getSetting 返回值后解密"
      pattern: "decrypt.*cookies"
---

<objective>
完成 SEC-01 安全加固的核心部分：将 VPS 密码/私钥和抖音 Cookie 的存储全面加密。

**写入时加密（encrypt on write）：**
- `routes/vps.js` POST / — 新建 VPS 时加密 password/private_key
- `routes/settings.js` setSetting douyin_cookies — 保存时加密

**读取时解密（decrypt on read）：**
- `services/ssh.js` buildConfig() — SSH 连接时解密凭证
- `routes/settings.js` getCfg() — 返回给设置页时解密（用户可看到明文用于编辑）
- `services/task-manager.js` getCookies() — 使用 Cookie 时解密

**迁移现有数据：**
- `scripts/migrate-encrypt.js` — 一次性迁移脚本，将已存库中的明文转为加密格式

Purpose: 数据库文件泄露后，攻击者无法从 VPS 凭证或 Cookie 字段直接获取可用凭证。
Output: 修改后的 5 个文件（4 个修改 + 1 个新建），加密全链路贯通。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- services/crypto.js（Plan 02-01 已创建）-->
```js
const { encrypt, decrypt } = require('./crypto');
// 在 services/ 下引用
const { encrypt, decrypt } = require('../services/crypto');
// 在 routes/ 下引用
```

<!-- routes/vps.js 当前 INSERT 代码（第 115-127 行） -->
```js
db.prepare(`
  INSERT INTO vps (user_id, name, host, port, username, auth_type, password, private_key)
  VALUES (?, ?, ?, ?, ?, ?, ?, ?)
`).run(
  req.session.userId,
  input.name,
  input.host,
  input.port,
  input.username,
  input.auth_type,
  input.auth_type === 'password' ? input.password : null,
  input.auth_type === 'key' ? input.private_key : null
);
```
目标：对第 6、7 个参数（password 和 private_key）分别包裹 encrypt()：
```js
  input.auth_type === 'password' ? encrypt(input.password) : null,
  input.auth_type === 'key' ? encrypt(input.private_key) : null
```

<!-- services/ssh.js buildConfig()（第 10-22 行） -->
```js
function buildConfig(vps) {
  const config = {
    host: vps.host,
    port: parseInt(vps.port, 10) || 22,
    username: vps.username || 'root',
    readyTimeout: 15000,
    keepaliveInterval: 30000,
    keepaliveCountMax: 3,
  };
  if (vps.auth_type === 'key' && vps.private_key) config.privateKey = vps.private_key;
  else config.password = vps.password;
  return config;
}
```
目标：解密后赋值：
```js
  if (vps.auth_type === 'key' && vps.private_key) config.privateKey = decrypt(vps.private_key);
  else config.password = decrypt(vps.password);
```

<!-- routes/settings.js douyin_cookies 读写（第 50 行 + 第 116 行） -->
```js
// 读（getCfg，第 50 行附近）
douyin_cookies: getSetting(userId, 'douyin_cookies') || '',

// 写（第 116 行）
setSetting(req.session.userId, 'douyin_cookies', cookies);
```
目标：
- 读：`douyin_cookies: decrypt(getSetting(userId, 'douyin_cookies') || '') || ''`
- 写：`setSetting(req.session.userId, 'douyin_cookies', encrypt(cookies))`

<!-- services/task-manager.js getCookies()（第 200-202 行附近） -->
```js
// 当前（第 201 行）
return getSetting('douyin_cookies', userId) || '';
```
目标：
```js
return decrypt(getSetting('douyin_cookies', userId) || '') || '';
```

<!-- scripts/migrate-encrypt.js 逻辑（新建文件） -->
```
1. require('../db') 和 { encrypt, decrypt } from '../services/crypto'
2. 迁移 vps 表：SELECT id, password, private_key FROM vps
   - 若 password 不为 null 且不以 'enc:v1:' 开头：UPDATE SET password=encrypt(password) WHERE id=?
   - 若 private_key 不为 null 且不以 'enc:v1:' 开头：UPDATE SET private_key=encrypt(private_key) WHERE id=?
3. 迁移 settings 表：SELECT user_id, value FROM settings WHERE key='douyin_cookies'
   - 若 value 不为空且不以 'enc:v1:' 开头：UPDATE SET value=encrypt(value) WHERE user_id=? AND key='douyin_cookies'
4. 输出迁移统计
```

<!-- 注意事项：
  - routes/settings.js 有自己的 getSetting 函数（第 12-14 行），与 db.js 的 getSetting 不同
    routes/settings.js: getSetting(userId, key) — 参数顺序不同
    db.js: getSetting(key, userId)
    两者都需要 decrypt，但调用位置不同
  - testConnection（routes/vps.js /:id/test）读取 vps 对象后直接传给 sshService.testConnection(vps)
    testConnection 调用 buildConfig(vpsConfig)，所以 buildConfig 解密已覆盖
  - normalizeVpsInput 中 password 和 private_key 来自用户提交表单，是明文，应该在 INSERT 时加密
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 routes/vps.js — VPS 写入前加密凭证</name>
  <files>routes/vps.js</files>
  <action>
    **步骤 1**：在文件顶部（现有 require 块之后，约第 6 行后）添加 crypto 导入：
    ```js
    const { encrypt, decrypt } = require('../services/crypto');
    ```
    
    **步骤 2**：在 `router.post('/', ...)` 中找到 `db.prepare().run()` 调用（第 115-127 行），
    将 password 和 private_key 参数包裹 encrypt()：
    
    将：
    ```js
    input.auth_type === 'password' ? input.password : null,
    input.auth_type === 'key' ? input.private_key : null
    ```
    
    替换为：
    ```js
    input.auth_type === 'password' ? encrypt(input.password) : null,
    input.auth_type === 'key' ? encrypt(input.private_key) : null
    ```
    
    **注意**：只修改 POST /（新建VPS）的 run() 调用参数，不修改其他路由。
    现有无编辑 VPS 路由，所以不需要处理 UPDATE 情况。
  </action>
  <verify>node -e "require('./routes/vps'); console.log('vps route OK'); process.exit(0)"</verify>
  <done>
    1. `grep -n "require.*crypto" routes/vps.js` 有输出
    2. `grep -n "encrypt(input.password)\|encrypt(input.private_key)" routes/vps.js` 有输出
    3. `node -e "require('./routes/vps')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 services/ssh.js — SSH 连接前解密凭证</name>
  <files>services/ssh.js</files>
  <action>
    **步骤 1**：在文件顶部（第 1-2 行之后）添加 crypto 导入：
    ```js
    const { decrypt } = require('./crypto');
    ```
    
    **步骤 2**：在 `buildConfig(vps)` 函数中，将：
    ```js
    if (vps.auth_type === 'key' && vps.private_key) config.privateKey = vps.private_key;
    else config.password = vps.password;
    ```
    
    替换为：
    ```js
    if (vps.auth_type === 'key' && vps.private_key) config.privateKey = decrypt(vps.private_key);
    else config.password = decrypt(vps.password);
    ```
    
    **设计说明**：`decrypt()` 对不以 'enc:v1:' 开头的值原样返回，所以迁移前的明文值仍然可用。
  </action>
  <verify>node -e "require('./services/ssh'); console.log('ssh service OK'); process.exit(0)"</verify>
  <done>
    1. `grep -n "require.*crypto" services/ssh.js` 有输出
    2. `grep -n "decrypt(vps.private_key)\|decrypt(vps.password)" services/ssh.js` 有输出
    3. `node -e "require('./services/ssh')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 3: 修改 routes/settings.js — Douyin Cookie 加密存储</name>
  <files>routes/settings.js</files>
  <action>
    **步骤 1**：在文件顶部（现有 require 块之后）添加 crypto 导入：
    ```js
    const { encrypt, decrypt } = require('../services/crypto');
    ```
    
    **步骤 2**：在 `getCfg(userId)` 函数中（第 40 行附近），找到 `douyin_cookies` 的读取行：
    ```js
    douyin_cookies: getSetting(userId, 'douyin_cookies') || '',
    ```
    
    替换为：
    ```js
    douyin_cookies: decrypt(getSetting(userId, 'douyin_cookies') || '') || '',
    ```
    
    **步骤 3**：在 douyin_cookies 的写入位置（第 116 行附近）：
    ```js
    setSetting(req.session.userId, 'douyin_cookies', cookies);
    ```
    
    替换为：
    ```js
    setSetting(req.session.userId, 'douyin_cookies', cookies ? encrypt(cookies) : '');
    ```
    
    **注意**：`cookies` 在第 112 行已被 `String(...).trim()` 处理，若为空字符串则不加密直接存空。
  </action>
  <verify>node -e "require('./routes/settings'); console.log('settings route OK'); process.exit(0)"</verify>
  <done>
    1. `grep -n "require.*crypto" routes/settings.js` 有输出
    2. `grep -n "decrypt(getSetting" routes/settings.js` 有输出
    3. `grep -n "encrypt(cookies)" routes/settings.js` 有输出
    4. `node -e "require('./routes/settings')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 4: 修改 services/task-manager.js — getCookies 解密</name>
  <files>services/task-manager.js</files>
  <action>
    **步骤 1**：在文件顶部（现有 require 块末尾附近）查找 getSetting 的导入：
    ```js
    const { getSetting } = require('../db');
    ```
    
    修改为（在现有导入行上追加 crypto 导入，或在 require 块中添加新行）：
    在文件顶部已有的 require 列表中（约第 1-15 行），添加：
    ```js
    const { decrypt } = require('./crypto');
    ```
    
    **步骤 2**：在 `getCookies(task)` 函数或 getDouyinCookies 的调用处（第 200-202 行附近），
    找到：
    ```js
    return getSetting('douyin_cookies', userId) || '';
    ```
    
    替换为：
    ```js
    return decrypt(getSetting('douyin_cookies', userId) || '') || '';
    ```
    
    **注意**：确认第 201 行是唯一的 douyin_cookies getSetting 调用，grep 确认后修改。
  </action>
  <verify>node -e "const tm = require('./services/task-manager'); console.log('task-manager OK'); process.exit(0)"</verify>
  <done>
    1. `grep -n "require.*crypto" services/task-manager.js` 有输出
    2. `grep -n "decrypt.*getSetting.*douyin_cookies\|decrypt.*douyin" services/task-manager.js` 有输出
    3. `node -e "require('./services/task-manager')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 5: 创建 scripts/migrate-encrypt.js</name>
  <files>scripts/migrate-encrypt.js</files>
  <action>
    创建新文件 `scripts/migrate-encrypt.js`，内容如下：
    
    ```js
    #!/usr/bin/env node
    // 一次性迁移脚本：将数据库中现有明文凭证加密存储
    // 用法：ENCRYPTION_KEY=<hex> node scripts/migrate-encrypt.js
    // 已加密（enc:v1: 前缀）的值会被跳过，幂等执行
    
    const db = require('../db');
    const { encrypt } = require('../services/crypto');
    
    const PREFIX = 'enc:v1:';
    
    function needsEncrypt(value) {
      return value && !String(value).startsWith(PREFIX);
    }
    
    let migratedVps = 0;
    let migratedCookies = 0;
    
    // 迁移 vps 表
    const vpsList = db.prepare('SELECT id, password, private_key FROM vps').all();
    for (const vps of vpsList) {
      let changed = false;
      const updates = {};
      if (needsEncrypt(vps.password)) {
        updates.password = encrypt(vps.password);
        changed = true;
      }
      if (needsEncrypt(vps.private_key)) {
        updates.private_key = encrypt(vps.private_key);
        changed = true;
      }
      if (changed) {
        if (updates.password && updates.private_key) {
          db.prepare('UPDATE vps SET password=?, private_key=? WHERE id=?')
            .run(updates.password, updates.private_key, vps.id);
        } else if (updates.password) {
          db.prepare('UPDATE vps SET password=? WHERE id=?').run(updates.password, vps.id);
        } else {
          db.prepare('UPDATE vps SET private_key=? WHERE id=?').run(updates.private_key, vps.id);
        }
        migratedVps++;
      }
    }
    
    // 迁移 settings 表中的 douyin_cookies
    const cookieRows = db.prepare("SELECT user_id, value FROM settings WHERE key='douyin_cookies'").all();
    for (const row of cookieRows) {
      if (needsEncrypt(row.value)) {
        db.prepare("UPDATE settings SET value=? WHERE user_id=? AND key='douyin_cookies'")
          .run(encrypt(row.value), row.user_id);
        migratedCookies++;
      }
    }
    
    console.log(`迁移完成：${migratedVps} 个 VPS 凭证，${migratedCookies} 个 Cookie 已加密`);
    console.log('（已加密的记录被跳过，可安全重复执行）');
    process.exit(0);
    ```
  </action>
  <verify>node -e "console.log(require('fs').existsSync('./scripts/migrate-encrypt.js') ? 'EXISTS' : 'MISSING')"</verify>
  <done>
    1. `ls scripts/migrate-encrypt.js` 文件存在
    2. `node -e "require('fs').readFileSync('./scripts/migrate-encrypt.js','utf8')" | grep "enc:v1:"` 有输出（包含 PREFIX 常量）
  </done>
</task>

<task type="auto">
  <name>Task 6: 端到端验证</name>
  <files>services/crypto.js</files>
  <action>
    执行以下验证命令序列：
    
    1. 所有修改文件可加载：
       ```
       node -e "require('./routes/vps'); require('./services/ssh'); require('./routes/settings'); require('./services/task-manager'); console.log('ALL OK')"
       ```
    
    2. 验证 encrypt/decrypt 路径贯通：
       ```
       ENCRYPTION_KEY=$(node -e "process.stdout.write(require('crypto').randomBytes(32).toString('hex'))") node -e "
       const {encrypt, decrypt} = require('./services/crypto');
       // 模拟 VPS 写入流程
       const encPw = encrypt('my-vps-password');
       if (!encPw.startsWith('enc:v1:')) throw new Error('encrypt failed');
       // 模拟 SSH 读取解密
       if (decrypt(encPw) !== 'my-vps-password') throw new Error('decrypt failed');
       // 模拟明文兼容（已有明文记录）
       if (decrypt('plaintext-pw') !== 'plaintext-pw') throw new Error('passthrough failed');
       console.log('ENCRYPT/DECRYPT ALL PASS');
       "
       ```
    
    3. grep 验证关键修改点：
       ```
       grep -n "encrypt\|decrypt" routes/vps.js routes/settings.js services/ssh.js services/task-manager.js | grep -v "^Binary\|#" | head -20
       ```
    
    4. 迁移脚本存在：
       ```
       ls -la scripts/migrate-encrypt.js
       ```
  </action>
  <verify>node -e "require('./routes/vps'); require('./services/ssh'); require('./routes/settings'); require('./services/task-manager'); console.log('ALL OK'); process.exit(0)"</verify>
  <done>
    验证命令1输出 ALL OK，验证命令2输出 ENCRYPT/DECRYPT ALL PASS，迁移脚本存在。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env / ENCRYPTION_KEY | 主机环境变量，应限制为 root 或服务账号可读 |
| SQLite 数据库文件 | 已加密，泄露后无法直接获取明文凭证 |
| VPS /tmp/dy_ck_N.txt | 已由 Plan 02-02 处理，任务停止时清除 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-SEC-01 | Information Disclosure | db 明文凭证 | mitigate | AES-256-GCM 加密存储 |
| T-SEC-02 | Tampering | GCM tag 验证 | mitigate | 解密失败（tag 不匹配）会抛出错误，不返回错误数据 |
| T-SEC-03 | Denial of Service | ENCRYPTION_KEY 丢失 | accept | key 丢失后加密数据无法解密，需从备份恢复；文档中告知用户备份 key |
| T-SEC-04 | Information Disclosure | 明文过渡期 | accept | decrypt() 对无前缀值原样返回，迁移脚本运行后消除 |
</threat_model>

<verification>
1. `node -e "require('./routes/vps'); require('./services/ssh'); require('./routes/settings'); require('./services/task-manager')"` — 全部无报错
2. `grep -rn "encrypt\|decrypt" routes/vps.js services/ssh.js routes/settings.js services/task-manager.js` — 所有文件都有引用
3. `ls scripts/migrate-encrypt.js` — 文件存在
4. 设置 ENCRYPTION_KEY 后，加密/解密往返测试正确
5. 新建 VPS（通过 Web UI 或 curl），SQLite 中 vps.password / vps.private_key 字段以 'enc:v1:' 开头
6. 任务正常启动，SSH 连接成功（decrypt 透明解密）
</verification>

<success_criteria>
- [ ] routes/vps.js 导入 encrypt，POST / 写入前加密凭证
- [ ] services/ssh.js 导入 decrypt，buildConfig() 中解密后使用
- [ ] routes/settings.js 导入 encrypt/decrypt，Cookie 写入前加密、读取后解密
- [ ] services/task-manager.js 导入 decrypt，getSetting('douyin_cookies') 后解密
- [ ] scripts/migrate-encrypt.js 存在，可幂等迁移现有明文数据
- [ ] 所有修改模块加载无报错
- [ ] ENCRYPTION_KEY 存在时，完整加密/解密链路工作正常
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-hardening/02-03-SUMMARY.md` with:
- 各文件修改的行号和变更内容
- migrate-encrypt.js 的使用说明
- 是否发现任何额外的明文凭证读取点（未覆盖的）
</output>
