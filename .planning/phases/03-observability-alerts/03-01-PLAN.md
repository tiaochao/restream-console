---
phase: 03-observability-alerts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/notifier.js
autonomous: true
requirements: [OBS-01]

must_haves:
  truths:
    - "services/notifier.js 导出 send(userId, event) 和 test(userId) 两个函数"
    - "send() 从 settings 表读取 notify_webhook_url / notify_telegram_token / notify_telegram_chat_id"
    - "send() 发送失败时静默降级（catch 日志，不 throw）"
    - "Webhook 发送：POST JSON {type,taskId,taskName,message,ts} 到配置的 URL"
    - "Telegram 发送：POST 到 Bot API sendMessage，chat_id 和 text 字段"
    - "两个渠道均未配置时 send() 立即返回，无任何网络请求"
    - "node -e \"require('./services/notifier')\" 无报错"
  artifacts:
    - path: "services/notifier.js"
      provides: "send(userId, event) / test(userId) — 双渠道通知，静默降级"
  key_links:
    - from: "services/notifier.js send()"
      to: "settings.notify_webhook_url"
      via: "getSetting(userId, 'notify_webhook_url')"
      pattern: "notify_webhook_url"
    - from: "services/notifier.js send()"
      to: "Telegram Bot API"
      via: "https://api.telegram.org/bot${token}/sendMessage"
      pattern: "telegram"
---

<objective>
创建 `services/notifier.js`：通用通知发送模块，支持 Webhook 和 Telegram 两种渠道。
从 `settings` 表读取配置，发送失败时静默降级（仅打印日志），不影响主流程。

Purpose: Phase 3 的基础设施，后续 03-03 将在 task-manager 状态机中调用 notifier.send()。
Output: 新文件 services/notifier.js，导出 send 和 test 函数。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- settings 表的通知配置 key -->
- notify_webhook_url    — Webhook URL（POST JSON），为空则不发送
- notify_telegram_token — Telegram Bot Token（格式：123456:ABC-xxx）
- notify_telegram_chat_id — Telegram Chat ID（数字或 @channelname）

<!-- 事件格式（task-manager 调用时传入） -->
```json
{
  "type": "task_stalled | task_restarting | task_error | task_recovered | test",
  "taskId": 42,
  "taskName": "我的推流任务",
  "message": "任务 我的推流任务 已掉线",
  "ts": 1715174400000
}
```

<!-- Webhook POST 格式 -->
POST {webhook_url}
Content-Type: application/json
Body: { type, taskId, taskName, message, ts, service: "restream-console" }

<!-- Telegram sendMessage API -->
POST https://api.telegram.org/bot{token}/sendMessage
Content-Type: application/json
Body: { chat_id, text: "🔴 任务掉线\n{message}\n\n任务ID: {taskId}", parse_mode: "Markdown" }

<!-- Node.js 内置 https 模块发送 JSON POST -->
```js
function postJson(url, body) {
  return new Promise((resolve, reject) => {
    const json = JSON.stringify(body);
    const parsed = new URL(url);
    const opts = {
      hostname: parsed.hostname,
      port: parsed.port || (parsed.protocol === 'https:' ? 443 : 80),
      path: parsed.pathname + parsed.search,
      method: 'POST',
      headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(json) },
      timeout: 10000,
    };
    const lib = parsed.protocol === 'https:' ? require('https') : require('http');
    const req = lib.request(opts, (res) => {
      res.resume(); // 消费响应体
      resolve(res.statusCode);
    });
    req.on('error', reject);
    req.on('timeout', () => { req.destroy(); reject(new Error('timeout')); });
    req.write(json);
    req.end();
  });
}
```

<!-- Telegram 消息格式（不使用 Markdown 避免转义问题） -->
type 到 emoji 映射：
  task_stalled    → "🔴 任务掉线"
  task_restarting → "🔄 自动重启"
  task_error      → "❌ 任务错误"
  task_recovered  → "✅ 任务恢复"
  test            → "✅ 通知测试"
  其他            → "ℹ️ 通知"
-->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 创建 services/notifier.js</name>
  <files>services/notifier.js</files>
  <action>
    创建新文件 `services/notifier.js`，内容如下：
    
    ```js
    const { getSetting } = require('../db');
    const { decrypt } = require('./crypto');
    
    const TYPE_EMOJI = {
      task_stalled:    '🔴 任务掉线',
      task_restarting: '🔄 自动重启',
      task_error:      '❌ 任务错误',
      task_recovered:  '✅ 任务恢复',
      test:            '✅ 通知测试',
    };
    
    function postJson(url, body) {
      return new Promise((resolve, reject) => {
        const json = JSON.stringify(body);
        let parsed;
        try { parsed = new URL(url); } catch (_) { return reject(new Error('无效 URL')); }
        const opts = {
          hostname: parsed.hostname,
          port: parsed.port || (parsed.protocol === 'https:' ? 443 : 80),
          path: parsed.pathname + parsed.search,
          method: 'POST',
          headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(json) },
          timeout: 10000,
        };
        const lib = parsed.protocol === 'https:' ? require('https') : require('http');
        const req = lib.request(opts, (res) => { res.resume(); resolve(res.statusCode); });
        req.on('error', reject);
        req.on('timeout', () => { req.destroy(); reject(new Error('请求超时')); });
        req.write(json);
        req.end();
      });
    }
    
    async function sendWebhook(webhookUrl, event) {
      const code = await postJson(webhookUrl, { ...event, service: 'restream-console' });
      if (code >= 400) console.warn(`[notifier] Webhook 响应 ${code}: ${webhookUrl}`);
    }
    
    async function sendTelegram(token, chatId, event) {
      const label = TYPE_EMOJI[event.type] || 'ℹ️ 通知';
      const lines = [label];
      if (event.message) lines.push(event.message);
      if (event.taskName) lines.push(`任务：${event.taskName}`);
      const text = lines.join('\n');
      const url = `https://api.telegram.org/bot${token}/sendMessage`;
      const code = await postJson(url, { chat_id: chatId, text });
      if (code >= 400) console.warn(`[notifier] Telegram 响应 ${code}`);
    }
    
    async function send(userId, event) {
      if (!userId && !event) return;
      const evt = { ts: Date.now(), ...event };
    
      const webhookUrl = userId ? getSetting('notify_webhook_url', userId) : null;
      const tgToken    = userId ? decrypt(getSetting('notify_telegram_token', userId) || '') || '' : '';
      const tgChatId   = userId ? getSetting('notify_telegram_chat_id', userId) : null;
    
      const tasks = [];
      if (webhookUrl) tasks.push(sendWebhook(webhookUrl, evt).catch(e => console.warn('[notifier] Webhook 失败:', e.message)));
      if (tgToken && tgChatId) tasks.push(sendTelegram(tgToken, tgChatId, evt).catch(e => console.warn('[notifier] Telegram 失败:', e.message)));
    
      await Promise.all(tasks);
    }
    
    async function test(userId) {
      return send(userId, { type: 'test', message: '这是一条来自 restream-console 的测试通知', taskName: '', taskId: null });
    }
    
    module.exports = { send, test };
    ```
    
    **设计要点：**
    - `send()` 对空配置立即返回（no-op），无网络请求
    - 每个渠道独立 catch，一个失败不影响另一个
    - `postJson()` 使用 Node.js 内置 https/http，无外部依赖
    - Telegram token 用 decrypt() 包裹（Phase 2 加密体系兼容）
    - 10s 请求超时，避免阻塞
  </action>
  <verify>node -e "const n = require('./services/notifier'); console.log(typeof n.send === 'function' && typeof n.test === 'function' ? 'OK' : 'FAIL'); process.exit(0)"</verify>
  <done>
    1. `services/notifier.js` 文件存在
    2. `node -e "const n = require('./services/notifier'); console.log(typeof n.send === 'function' && typeof n.test === 'function' ? 'OK' : 'FAIL')"` 输出 OK
    3. `grep -n "notify_webhook_url\|sendTelegram\|postJson" services/notifier.js` 有输出
  </done>
</task>

<task type="auto">
  <name>Task 2: 验证 notifier.js</name>
  <files>services/notifier.js</files>
  <action>
    执行验证命令：
    
    1. 模块加载：
       ```
       node -e "require('./services/notifier'); console.log('notifier OK')"
       ```
    
    2. 导出函数类型：
       ```
       node -e "const n = require('./services/notifier'); console.log(typeof n.send, typeof n.test)"
       ```
       期望：`function function`
    
    3. 空配置时 send() 不发起网络请求（直接返回）：
       ```
       node -e "
       const n = require('./services/notifier');
       n.send(null, {type:'test',message:'x'}).then(() => console.log('no-op OK')).catch(console.error);
       "
       ```
       期望：`no-op OK`（无网络请求报错）
  </action>
  <verify>node -e "const n = require('./services/notifier'); n.send(null, {type:'test'}).then(() => console.log('OK')).catch(e => { console.error(e); process.exit(1); })"</verify>
  <done>
    所有验证输出正确，notifier 模块加载成功。
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('./services/notifier')"` — 无报错
2. `grep -n "send\|test\|webhook\|telegram" services/notifier.js` — 有输出
3. send(null, ...) 不抛出错误
</verification>

<success_criteria>
- [ ] services/notifier.js 存在，导出 send 和 test
- [ ] send() 读取三个 settings key（notify_webhook_url / notify_telegram_token / notify_telegram_chat_id）
- [ ] 发送失败时 catch 并打印警告，不 throw
- [ ] 两个渠道均无配置时直接返回（no-op）
- [ ] 模块加载无报错
</success_criteria>

<output>
After completion, create `.planning/phases/03-observability-alerts/03-01-SUMMARY.md` with:
- notifier.js 的行号范围
- send() 和 test() 的函数签名
- 使用的 settings key 名称
</output>
