---
phase: 03-observability-alerts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/settings.js
  - views/settings.ejs
autonomous: true
requirements: [OBS-01]

must_haves:
  truths:
    - "getCfg() 返回对象中包含 notify_webhook_url / notify_telegram_token / notify_telegram_chat_id"
    - "POST /settings/notifications 路由存在，保存三个通知配置项"
    - "POST /settings/test-notify 路由存在，调用 notifier.test() 并返回 JSON"
    - "views/settings.ejs 中存在通知渠道配置 card，含 Webhook URL 输入框和 Telegram 两字段"
    - "settings.ejs 存在测试发送按钮，通过 fetch 调用 /settings/test-notify"
    - "node -e \"require('./routes/settings')\" 无报错"
  artifacts:
    - path: "routes/settings.js"
      provides: "通知配置的 GET（getCfg）、POST /notifications 保存、POST /test-notify 测试"
    - path: "views/settings.ejs"
      provides: "通知渠道配置 UI 卡片"
  key_links:
    - from: "POST /settings/notifications"
      to: "settings.notify_webhook_url / notify_telegram_token / notify_telegram_chat_id"
      via: "setSetting(userId, key, value)"
      pattern: "notify_"
    - from: "POST /settings/test-notify"
      to: "services/notifier.js test()"
      via: "notifier.test(userId)"
      pattern: "test-notify"
---

<objective>
为通知渠道配置添加后端路由和前端 UI：
1. `routes/settings.js`：扩展 getCfg 包含通知配置，添加 POST /notifications 和 POST /test-notify 路由
2. `views/settings.ejs`：在最后一个设置卡片之后、`<script>` 标签之前插入通知配置卡片

Purpose: 让用户能在设置页面配置 Webhook URL 和 Telegram Bot，并点击测试按钮验证配置。
Output: 修改后的 routes/settings.js 和 views/settings.ejs。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- settings.js 当前 getCfg 末尾（第 51 行）-->
```js
douyin_cookies: decrypt(getSetting(userId, 'douyin_cookies') || '') || '',
```
目标：在其后追加：
```js
notify_webhook_url:        getSetting(userId, 'notify_webhook_url')      || '',
notify_telegram_token:     getSetting(userId, 'notify_telegram_token')   || '',
notify_telegram_chat_id:   getSetting(userId, 'notify_telegram_chat_id') || '',
```

<!-- 新增 POST /settings/notifications 路由逻辑 -->
```js
router.post('/notifications', (req, res) => {
  const webhookUrl = String(req.body.notify_webhook_url || '').trim();
  const tgToken    = String(req.body.notify_telegram_token || '').trim();
  const tgChatId   = String(req.body.notify_telegram_chat_id || '').trim();
  
  if (webhookUrl && !/^https?:\/\/.+/i.test(webhookUrl)) {
    return renderSettings(req, res, { status: 400, error: 'Webhook URL 格式无效（需 http/https 开头）' });
  }
  
  setSetting(req.session.userId, 'notify_webhook_url',      webhookUrl);
  setSetting(req.session.userId, 'notify_telegram_token',   tgToken);
  setSetting(req.session.userId, 'notify_telegram_chat_id', tgChatId);
  renderSettings(req, res, { success: '通知配置已保存' });
});
```

<!-- 新增 POST /settings/test-notify 路由 -->
```js
router.post('/test-notify', async (req, res) => {
  try {
    await notifier.test(req.session.userId);
    res.json({ ok: true, msg: '测试通知已发送（如未收到，请检查配置）' });
  } catch (e) {
    res.json({ ok: false, msg: e.message });
  }
});
```

<!-- settings.js 顶部添加 notifier 引用 -->
```js
const notifier = require('../services/notifier');
```

<!-- views/settings.ejs 新增的通知卡片 HTML（插入位置：</div>（最后一个 card 的闭合）与 <script> 之间） -->
插入位置：第 228 行（</div>\n\n<script> 之间）
新增内容：一个 <div class="card"> 块，包含：
- card-header: "告警通知"
- Webhook URL 文本输入框
- Telegram Bot Token 文本输入框
- Telegram Chat ID 文本输入框
- "保存通知配置"提交按钮（action="/settings/notifications"）
- "发送测试通知"按钮（onclick="testNotify()"，JS fetch POST /settings/test-notify）
- 结果显示 span

<!-- testNotify JS 函数（添加到 <script> 块末尾，</script> 之前） -->
```js
async function testNotify() {
  const btn = document.getElementById('test-notify-btn');
  const result = document.getElementById('test-notify-result');
  btn.disabled = true; btn.textContent = '发送中...';
  result.textContent = '';
  try {
    const r = await fetch('/settings/test-notify', {
      method: 'POST',
      headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
      body: '_csrf=' + encodeURIComponent(_csrf),
    });
    const d = await r.json();
    result.textContent = d.msg;
    result.style.color = d.ok ? 'var(--text-3)' : '#f87171';
  } catch (e) {
    result.textContent = '请求失败';
    result.style.color = '#f87171';
  }
  btn.disabled = false; btn.textContent = '发送测试通知';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 routes/settings.js — 添加通知配置路由</name>
  <files>routes/settings.js</files>
  <action>
    **步骤 1**：在文件顶部（require 块末尾）添加 notifier 引用：
    ```js
    const notifier = require('../services/notifier');
    ```
    
    **步骤 2**：在 `getCfg(userId)` 函数的返回对象中（`douyin_cookies` 行之后），追加：
    ```js
    notify_webhook_url:        getSetting(userId, 'notify_webhook_url')      || '',
    notify_telegram_token:     getSetting(userId, 'notify_telegram_token')   || '',
    notify_telegram_chat_id:   getSetting(userId, 'notify_telegram_chat_id') || '',
    ```
    
    **步骤 3**：在文件末尾（`module.exports` 之前或最后一个路由之后）添加两个新路由：
    
    ```js
    router.post('/notifications', (req, res) => {
      const webhookUrl = String(req.body.notify_webhook_url || '').trim();
      const tgToken    = String(req.body.notify_telegram_token || '').trim();
      const tgChatId   = String(req.body.notify_telegram_chat_id || '').trim();
    
      if (webhookUrl && !/^https?:\/\/.+/i.test(webhookUrl)) {
        return renderSettings(req, res, { status: 400, error: 'Webhook URL 格式无效（需 http/https 开头）' });
      }
    
      setSetting(req.session.userId, 'notify_webhook_url',      webhookUrl);
      setSetting(req.session.userId, 'notify_telegram_token',   tgToken);
      setSetting(req.session.userId, 'notify_telegram_chat_id', tgChatId);
      renderSettings(req, res, { success: '通知配置已保存' });
    });
    
    router.post('/test-notify', async (req, res) => {
      try {
        await notifier.test(req.session.userId);
        res.json({ ok: true, msg: '测试通知已发送（如未收到，请检查配置和 Bot 设置）' });
      } catch (e) {
        res.json({ ok: false, msg: e.message });
      }
    });
    ```
    
    **注意**：settings.js 末尾有 `module.exports = router;`，在其**之前**插入新路由。
  </action>
  <verify>node -e "require('./routes/settings'); console.log('settings OK'); process.exit(0)"</verify>
  <done>
    1. `grep -n "notify_webhook_url" routes/settings.js` 有输出（getCfg + 路由中）
    2. `grep -n "test-notify\|test_notify" routes/settings.js` 有输出
    3. `grep -n "notifier" routes/settings.js` 有输出
    4. `node -e "require('./routes/settings')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 2: 修改 views/settings.ejs — 添加通知配置卡片</name>
  <files>views/settings.ejs</files>
  <action>
    在 `views/settings.ejs` 中，找到 `<script>` 标签开始位置（约第 229 行：`<script>`），
    在其**前**插入以下通知配置卡片 HTML：
    
    ```html
    <!-- 告警通知配置 -->
    <div class="card">
      <div class="card-header">
        <div class="card-title">告警通知</div>
      </div>
      <div class="modal-body stack">
        <div class="alert alert-info" style="font-size:13px;">
          任务掉线、自动重启、恢复正常等关键状态变更时自动推送告警。支持 Webhook（飞书/钉钉/企微/自定义）和 Telegram Bot 两种渠道，均可选填。
        </div>
        <form method="POST" action="/settings/notifications" class="stack">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <div>
            <label class="form-label">Webhook URL</label>
            <input type="url" name="notify_webhook_url" value="<%= cfg.notify_webhook_url %>"
              placeholder="https://example.com/webhook（飞书/钉钉/企微/自定义）"
              class="form-input" autocomplete="off">
            <div class="form-hint">状态变更时向此地址 POST JSON，留空则不发送</div>
          </div>
          <div style="display:grid;grid-template-columns:1fr 1fr;gap:16px;">
            <div>
              <label class="form-label">Telegram Bot Token</label>
              <input type="text" name="notify_telegram_token" value="<%= cfg.notify_telegram_token %>"
                placeholder="123456:ABC-DEFxxx"
                class="form-input" autocomplete="off">
            </div>
            <div>
              <label class="form-label">Telegram Chat ID</label>
              <input type="text" name="notify_telegram_chat_id" value="<%= cfg.notify_telegram_chat_id %>"
                placeholder="数字 Chat ID 或 @频道名"
                class="form-input" autocomplete="off">
            </div>
          </div>
          <div class="form-hint">Telegram：在 @BotFather 创建 Bot 并获取 Token，Chat ID 可通过 @userinfobot 获取</div>
          <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap;">
            <button type="submit" class="btn btn-secondary">保存通知配置</button>
            <button type="button" id="test-notify-btn" onclick="testNotify()" class="btn btn-secondary">发送测试通知</button>
            <span id="test-notify-result" style="font-size:13px;"></span>
          </div>
        </form>
      </div>
    </div>
    
    ```
    
    **然后**在 `<script>` 块中，在 `</script>` 之前（即 `setupDouyinBookmarklet();` 等现有代码之后），
    追加 testNotify 函数：
    
    ```js
    async function testNotify() {
      const btn = document.getElementById('test-notify-btn');
      const result = document.getElementById('test-notify-result');
      btn.disabled = true; btn.textContent = '发送中...';
      result.textContent = '';
      try {
        const r = await fetch('/settings/test-notify', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: '_csrf=' + encodeURIComponent(_csrf),
        });
        const d = await r.json();
        result.textContent = d.msg;
        result.style.color = d.ok ? 'var(--text-3)' : '#f87171';
      } catch (e) {
        result.textContent = '请求失败';
        result.style.color = '#f87171';
      }
      btn.disabled = false; btn.textContent = '发送测试通知';
    }
    ```
    
    **注意**：不修改现有设置项，只在两个位置插入内容。
  </action>
  <verify>node -e "const fs=require('fs'); const c=fs.readFileSync('./views/settings.ejs','utf8'); console.log(c.includes('notify_webhook_url') && c.includes('testNotify') ? 'OK' : 'FAIL')"</verify>
  <done>
    1. `grep -n "notify_webhook_url\|testNotify\|test-notify" views/settings.ejs` 有输出（卡片 + JS 函数）
    2. `grep -n "告警通知" views/settings.ejs` 有输出
    3. settings.ejs 原有内容（Cookie 卡片、任务调度卡片等）保持不变
  </done>
</task>

<task type="auto">
  <name>Task 3: 端到端验证</name>
  <files>routes/settings.js, views/settings.ejs</files>
  <action>
    执行以下验证命令：
    
    1. 路由模块加载：
       ```
       node -e "require('./routes/settings'); console.log('settings route OK')"
       ```
    
    2. 确认三个 notify key 在 getCfg 中：
       ```
       grep -n "notify_webhook_url\|notify_telegram" routes/settings.js
       ```
    
    3. 确认两个新路由存在：
       ```
       grep -n "\/notifications\|\/test-notify" routes/settings.js
       ```
    
    4. 确认 settings.ejs 包含新卡片：
       ```
       grep -n "notify_webhook_url\|testNotify\|告警通知" views/settings.ejs
       ```
  </action>
  <verify>node -e "require('./routes/settings'); console.log('OK'); process.exit(0)"</verify>
  <done>
    所有验证命令有输出，模块加载成功输出 OK。
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('./routes/settings')"` — 无报错
2. `grep -c "notify_webhook_url" routes/settings.js` — >= 2（getCfg + 路由）
3. `grep -n "告警通知\|testNotify" views/settings.ejs` — 有输出
</verification>

<success_criteria>
- [ ] getCfg() 包含 notify_webhook_url, notify_telegram_token, notify_telegram_chat_id
- [ ] POST /settings/notifications 路由存在，验证 Webhook URL 格式
- [ ] POST /settings/test-notify 路由存在，调用 notifier.test()
- [ ] views/settings.ejs 有通知配置卡片（Webhook + Telegram 两个渠道）
- [ ] settings.ejs 有 testNotify() JS 函数，通过 fetch 调用 test-notify
- [ ] 模块加载无报错
</success_criteria>

<output>
After completion, create `.planning/phases/03-observability-alerts/03-02-SUMMARY.md` with:
- routes/settings.js 新增路由的行号
- getCfg 新增字段的行号
- views/settings.ejs 新增卡片的行号范围
</output>
