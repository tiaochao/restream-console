---
phase: 04-observability-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/dashboard.js
  - views/partials/stats.ejs
  - server.js
autonomous: true
requirements: [OBS-02, OBS-03]

must_haves:
  truths:
    - "routes/dashboard.js getStats() 包含 apiKeyHealth 字段（total/active/quota/invalid）"
    - "views/partials/stats.ejs 有 YouTube API Key 健康状态卡片"
    - "server.js 末尾有 4 参数全局错误处理中间件 (err, req, res, next)"
    - "node -e \"require('./routes/dashboard')\" 无报错"
    - "node -e \"require('./server')\" 行为不变（模块加载无报错）"
  artifacts:
    - path: "routes/dashboard.js"
      provides: "getApiKeyHealth() 函数 + getStats() 扩展 apiKeyHealth"
    - path: "views/partials/stats.ejs"
      provides: "API Key 健康状态卡片"
    - path: "server.js"
      provides: "全局错误处理中间件（4 参数 Express 错误处理器）"
  key_links:
    - from: "routes/dashboard.js getStats()"
      to: "getSetting('youtube_api_keys')"
      via: "parseApiKeys() + JSON.parse(youtube_api_key_status)"
      pattern: "apiKeyHealth"
    - from: "server.js"
      to: "全局 (err, req, res, next) 中间件"
      via: "app.use((err, req, res, next) => {...})"
      pattern: "error_handler"
---

<objective>
扩展 Dashboard 显示 YouTube API Key 池健康状态，并在 server.js 添加全局错误处理中间件。

**两个独立任务：**
1. Dashboard API Key 健康卡片 — 显示可用/配额耗尽/无效 Key 数量
2. 全局错误处理中间件 — 捕获未处理异常，生产环境返回通用 JSON，不暴露堆栈

Purpose: 让运维者在首页一眼看到 API Key 配额状况；捕获未处理异常避免 500 明文堆栈泄露。
Output: 修改后的 routes/dashboard.js、views/partials/stats.ejs、server.js。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- routes/dashboard.js 现有 getStats() 函数末尾 -->
在 `return { totalVps, onlineVps, runningTasks, retryingTasks, errorTasks, totalTasks };` 之前，
先添加 getApiKeyHealth 函数：
```js
function getApiKeyHealth(userId) {
  function parseKeys(raw) {
    return (raw || '').split(/[\n,]+/).map(s => s.trim()).filter(Boolean);
  }
  const { getSetting } = require('../db');
  const pooled = parseKeys(getSetting('youtube_api_keys', userId));
  const legacy = parseKeys(getSetting('youtube_api_key', userId));
  const keys = [...new Set([...pooled, ...legacy])];
  const statusMap = JSON.parse(getSetting('youtube_api_key_status', userId) || '{}') || {};
  const total = keys.length;
  const quota = keys.filter(k => statusMap[k]?.status === 'quota' || statusMap[k]?.status === 'rate_limited').length;
  const invalid = keys.filter(k => ['invalid', 'forbidden'].includes(statusMap[k]?.status)).length;
  return { total, active: Math.max(0, total - quota - invalid), quota, invalid };
}
```

然后在 getStats() 末尾添加 apiKeyHealth：
```js
function getStats(userId) {
  // ...现有代码...
  const apiKeyHealth = getApiKeyHealth(userId);
  return { totalVps, onlineVps, runningTasks, retryingTasks, errorTasks, totalTasks, apiKeyHealth };
}
```

<!-- views/partials/stats.ejs — 在第四个 stat-card（总任务数）之后插入 API Key 卡片 -->
```html
  <div class="stat-card">
    <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;">
      <span class="stat-label">API Key 池</span>
      <span style="width:32px;height:32px;background:rgba(99,102,241,0.12);border-radius:8px;display:flex;align-items:center;justify-content:center;">
        <svg width="16" height="16" fill="none" stroke="#818cf8" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"/></svg>
      </span>
    </div>
    <% const kh = stats.apiKeyHealth; %>
    <div class="stat-value" style="color:<%= kh && kh.active > 0 ? '#818cf8' : '#f87171' %>">
      <%= kh ? kh.active : '--' %><% if (kh && kh.total > 0) { %>/<%= kh.total %><% } %>
    </div>
    <div style="font-size:11px;color:var(--text-2);margin-top:6px;">
      <% if (kh && kh.total === 0) { %>
        未配置 API Key
      <% } else if (kh) { %>
        可用<% if (kh.quota > 0) { %>，<%= kh.quota %> 个配额耗尽<% } %><% if (kh.invalid > 0) { %>，<%= kh.invalid %> 个无效<% } %>
      <% } %>
    </div>
  </div>
```

<!-- server.js 全局错误处理中间件（在最后一个 app.use 路由之后，app.listen 之前）-->
```js
// 全局错误处理中间件（必须放在所有路由之后，app.listen 之前）
app.use((err, req, res, next) => {
  const status = err.status || err.statusCode || 500;
  console.error(`[ERROR] ${req.method} ${req.path}:`, err.message, err.stack ? err.stack.split('\n')[1] : '');
  if (req.xhr || (req.headers.accept || '').includes('application/json')) {
    return res.status(status).json({ error: isProduction ? '服务器内部错误' : err.message });
  }
  res.status(status).send(isProduction ? '<h1>服务器内部错误</h1>' : `<pre>${err.message}\n${err.stack || ''}</pre>`);
});
```
插入位置：`app.get('/', (req, res) => res.redirect('/dashboard'));` 之后，`const PORT = ...` 之前。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: routes/dashboard.js — 添加 API Key 健康统计</name>
  <files>routes/dashboard.js</files>
  <action>
    **步骤 1**：在 `function getStats(userId) {` 之前，添加 getApiKeyHealth 函数：
    ```js
    function getApiKeyHealth(userId) {
      function parseKeys(raw) {
        return (raw || '').split(/[\n,]+/).map(s => s.trim()).filter(Boolean);
      }
      const { getSetting } = require('../db');
      const pooled = parseKeys(getSetting('youtube_api_keys', userId));
      const legacy = parseKeys(getSetting('youtube_api_key', userId));
      const keys = [...new Set([...pooled, ...legacy])];
      const statusMap = JSON.parse(getSetting('youtube_api_key_status', userId) || '{}') || {};
      const total = keys.length;
      const quota = keys.filter(k => statusMap[k]?.status === 'quota' || statusMap[k]?.status === 'rate_limited').length;
      const invalid = keys.filter(k => ['invalid', 'forbidden'].includes(statusMap[k]?.status)).length;
      return { total, active: Math.max(0, total - quota - invalid), quota, invalid };
    }
    ```
    
    **步骤 2**：在 `getStats()` 函数中，找到 `return { totalVps, ... };` 这一行，修改为：
    ```js
    const apiKeyHealth = getApiKeyHealth(userId);
    return { totalVps, onlineVps, runningTasks, retryingTasks, errorTasks, totalTasks, apiKeyHealth };
    ```
  </action>
  <verify>node -e "require('./routes/dashboard'); console.log('OK')" 2>&1</verify>
  <done>
    1. `grep -n "getApiKeyHealth\|apiKeyHealth" routes/dashboard.js` 有输出
    2. `node -e "require('./routes/dashboard')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 2: views/partials/stats.ejs — 添加 API Key 健康卡片</name>
  <files>views/partials/stats.ejs</files>
  <action>
    在 `<!-- 任务状态表 -->` 注释之前（即统计卡片 section `</div>` 闭合前），插入 API Key 健康卡片：
    
    ```html
      <div class="stat-card">
        <div style="display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;">
          <span class="stat-label">API Key 池</span>
          <span style="width:32px;height:32px;background:rgba(99,102,241,0.12);border-radius:8px;display:flex;align-items:center;justify-content:center;">
            <svg width="16" height="16" fill="none" stroke="#818cf8" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.8" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z"/></svg>
          </span>
        </div>
        <% const kh = stats.apiKeyHealth; %>
        <div class="stat-value" style="color:<%= kh && kh.active > 0 ? '#818cf8' : '#f87171' %>">
          <%= kh ? kh.active : '--' %><% if (kh && kh.total > 0) { %>/<%= kh.total %><% } %>
        </div>
        <div style="font-size:11px;color:var(--text-2);margin-top:6px;">
          <% if (kh && kh.total === 0) { %>
            未配置 API Key
          <% } else if (kh) { %>
            可用<% if (kh.quota > 0) { %>，<%= kh.quota %> 个配额耗尽<% } %><% if (kh.invalid > 0) { %>，<%= kh.invalid %> 个无效<% } %>
          <% } %>
        </div>
      </div>
    ```
    
    插入位置：找到 `<!-- 任务状态表 -->` 注释行，在其**之前**（但在外层 section `</div>` 之后）插入。
    实际上是在 `</div>` 闭合了 stats.section div 之后、`<!-- 任务状态表 -->` 之前。
    
    注意：检查 stats.ejs 的实际结构，找到 4 个 stat-card 之后的 `</div>` 位置，在这个闭合标签**之前**插入新卡片。
  </action>
  <verify>node -e "const fs=require('fs'); const c=fs.readFileSync('./views/partials/stats.ejs','utf8'); console.log(c.includes('apiKeyHealth') || c.includes('kh.active') ? 'OK' : 'FAIL')"</verify>
  <done>
    1. `grep -n "apiKeyHealth\|API Key 池\|kh.active" views/partials/stats.ejs` 有输出
    2. EJS 模板语法正确（无明显语法错误）
  </done>
</task>

<task type="auto">
  <name>Task 3: server.js — 添加全局错误处理中间件</name>
  <files>server.js</files>
  <action>
    在 `app.get('/', (req, res) => res.redirect('/dashboard'));` 这一行之后，`const PORT = ...` 之前，
    插入全局错误处理中间件：
    
    ```js
    app.use((err, req, res, next) => {
      const status = err.status || err.statusCode || 500;
      console.error(`[ERROR] ${req.method} ${req.path}:`, err.message, err.stack ? err.stack.split('\n')[1] : '');
      if (req.xhr || (req.headers.accept || '').includes('application/json')) {
        return res.status(status).json({ error: isProduction ? '服务器内部错误' : err.message });
      }
      res.status(status).send(isProduction ? '<h1>服务器内部错误</h1>' : `<pre>${err.message}\n${err.stack || ''}</pre>`);
    });
    ```
    
    **注意**：4 参数中间件 `(err, req, res, next)` 必须恰好有 4 个参数，Express 才将其识别为错误处理器。
    不要省略 next 参数。
  </action>
  <verify>node -e "require('./server')" 2>&1 | head -5</verify>
  <done>
    1. `grep -n "err, req, res, next" server.js` 有输出
    2. `grep -n "ERROR.*req.method\|服务器内部错误" server.js` 有输出
  </done>
</task>

<task type="auto">
  <name>Task 4: 端到端验证</name>
  <files>routes/dashboard.js, views/partials/stats.ejs, server.js</files>
  <action>
    1. `node -e "require('./routes/dashboard'); console.log('dashboard OK')"`
    2. `grep -n "apiKeyHealth\|getApiKeyHealth" routes/dashboard.js`
    3. `grep -n "API Key 池\|kh.active" views/partials/stats.ejs`
    4. `grep -n "err, req, res, next" server.js`
  </action>
  <verify>node -e "require('./routes/dashboard'); console.log('OK'); process.exit(0)"</verify>
  <done>
    所有验证命令有输出，dashboard 路由加载成功。
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('./routes/dashboard')"` — 无报错
2. `grep -c "apiKeyHealth" routes/dashboard.js` — >= 2（函数定义 + return）
3. `grep -n "API Key 池" views/partials/stats.ejs` — 有输出
4. `grep -n "err, req, res, next" server.js` — 有输出
</verification>

<success_criteria>
- [ ] routes/dashboard.js 有 getApiKeyHealth() 函数
- [ ] getStats() 返回值包含 apiKeyHealth 字段
- [ ] views/partials/stats.ejs 有 API Key 健康卡片（显示可用数/总数/配额耗尽）
- [ ] server.js 有 4 参数错误处理中间件，放置在所有路由之后
- [ ] 模块加载无报错
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability-dashboard/04-02-SUMMARY.md` with:
- getApiKeyHealth 函数行号
- stats.ejs 新卡片行号范围
- server.js 错误处理中间件行号
</output>
