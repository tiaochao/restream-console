---
phase: 04-observability-dashboard
plan: 03
type: execute
wave: 2
depends_on: [04-01-PLAN.md]
files_modified:
  - routes/tasks.js
  - views/task-detail.ejs
autonomous: true
requirements: [OBS-02]

must_haves:
  truths:
    - "routes/tasks.js 有 GET /:id 路由，查询 task_events 生成历史统计"
    - "GET /tasks/:id 返回 task、stats（stall/restart/error/recovered 计数）和 recentEvents（最近 50 条）"
    - "views/task-detail.ejs 存在，展示任务名、状态、统计数字和事件历史表格"
    - "任务列表 views/tasks.ejs 中每行有链接到 /tasks/:id 的「详情」入口"
    - "node -e \"require('./routes/tasks')\" 无报错"
  artifacts:
    - path: "routes/tasks.js"
      provides: "GET /tasks/:id — 任务详情 + 历史统计"
    - path: "views/task-detail.ejs"
      provides: "任务详情页 — 统计数字 + 事件历史"
  key_links:
    - from: "GET /tasks/:id"
      to: "task_events 表"
      via: "SELECT COUNT(*) WHERE task_id=? AND to_status='stalled'"
      pattern: "task_events"
---

<objective>
新增任务详情页，展示该任务的历史统计数据（掉线次数、重启次数、恢复次数）和最近 50 条状态转换事件。

**数据来源**：04-01 创建的 `task_events` 表。

**路由**：`GET /tasks/:id` — 查询任务行 + 统计 + 事件列表，渲染 `views/task-detail.ejs`。

**统计项：**
- stall_count: COUNT where to_status='stalled'（最近 30 天）
- restart_count: COUNT where to_status='restarting'（最近 30 天）
- error_count: COUNT where to_status='error'（最近 30 天）
- recovered_count: COUNT where to_status='running' AND from_status IN ('stalled','restarting','source_retrying')（最近 30 天）

Purpose: 让用户进入任务详情页能看到最近 30 天的掉线次数和总运行时长。
Output: routes/tasks.js 新路由 + views/task-detail.ejs 详情页。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- GET /tasks/:id 路由逻辑 -->
```js
router.get('/:id', (req, res) => {
  const taskId = parseInt(req.params.id, 10);
  if (!taskId || isNaN(taskId)) return res.status(400).send('无效任务 ID');
  
  const task = db.prepare(`
    SELECT t.*, v.name as vps_name
    FROM tasks t
    LEFT JOIN vps v ON t.vps_id = v.id
    WHERE t.id = ? AND t.user_id = ?
  `).get(taskId, req.session.userId);
  
  if (!task) return res.status(404).render('error', { title: '任务不存在', message: '找不到该任务或无权访问', currentPath: '/tasks' });
  
  const since = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString().replace('T', ' ').slice(0, 19);
  
  const statRow = db.prepare(`
    SELECT
      COUNT(CASE WHEN to_status='stalled' THEN 1 END) as stall_count,
      COUNT(CASE WHEN to_status='restarting' THEN 1 END) as restart_count,
      COUNT(CASE WHEN to_status='error' THEN 1 END) as error_count,
      COUNT(CASE WHEN to_status='running' AND from_status IN ('stalled','restarting','source_retrying') THEN 1 END) as recovered_count
    FROM task_events
    WHERE task_id = ? AND created_at >= ?
  `).get(taskId, since);
  
  const recentEvents = db.prepare(`
    SELECT id, from_status, to_status, reason, created_at
    FROM task_events
    WHERE task_id = ?
    ORDER BY created_at DESC
    LIMIT 50
  `).all(taskId);
  
  res.render('task-detail', {
    title: `任务详情 — ${task.name || task.id}`,
    currentPath: '/tasks',
    task,
    stats: statRow || { stall_count: 0, restart_count: 0, error_count: 0, recovered_count: 0 },
    recentEvents,
  });
});
```
**注意**：将此路由放在 POST `/:id/*` 路由**之前**，但在 `router.get('/', ...)` 之后，避免被通配路由捕获。
实际上由于 Express 路由匹配顺序，GET /:id 需要在 GET / 之后、所有 POST 路由之前。

<!-- views/task-detail.ejs -->
```html
<% /* layout: layout */ %>
<div class="page-header">
  <div>
    <div class="page-title"><%= task.name || ('任务 #' + task.id) %></div>
    <div class="page-subtitle">
      <a href="/tasks" class="link">任务管理</a> / 详情
    </div>
  </div>
  <a href="/tasks" class="btn btn-secondary">返回列表</a>
</div>

<!-- 统计卡片 -->
<div class="section" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:16px;">
  <div class="stat-card">
    <div class="stat-label">掉线次数（近30天）</div>
    <div class="stat-value" style="color:<%= stats.stall_count > 0 ? '#fbbf24' : 'var(--text-1)' %>"><%= stats.stall_count %></div>
  </div>
  <div class="stat-card">
    <div class="stat-label">自动重启（近30天）</div>
    <div class="stat-value" style="color:<%= stats.restart_count > 0 ? '#60a5fa' : 'var(--text-1)' %>"><%= stats.restart_count %></div>
  </div>
  <div class="stat-card">
    <div class="stat-label">进入错误（近30天）</div>
    <div class="stat-value" style="color:<%= stats.error_count > 0 ? '#f87171' : 'var(--text-1)' %>"><%= stats.error_count %></div>
  </div>
  <div class="stat-card">
    <div class="stat-label">成功恢复（近30天）</div>
    <div class="stat-value" style="color:<%= stats.recovered_count > 0 ? '#34d399' : 'var(--text-1)' %>"><%= stats.recovered_count %></div>
  </div>
</div>

<!-- 任务基本信息 -->
<div class="card">
  <div class="card-header"><div class="card-title">基本信息</div></div>
  <div class="modal-body stack" style="gap:12px;">
    <div style="display:grid;grid-template-columns:120px 1fr;gap:8px;font-size:14px;">
      <span style="color:var(--text-2);">状态</span><span><%= task.status %></span>
      <span style="color:var(--text-2);">VPS</span><span><%= task.vps_name || '--' %></span>
      <span style="color:var(--text-2);">来源</span><span class="monospace" style="font-size:12px;word-break:break-all;"><%= task.source_url %></span>
      <span style="color:var(--text-2);">启动时间</span><span><%= task.started_at || '--' %></span>
      <span style="color:var(--text-2);">最后活跃</span><span><%= task.last_active_at || '--' %></span>
      <span style="color:var(--text-2);">自动重启</span><span><%= task.auto_restart ? '已启用' : '关闭' %></span>
    </div>
  </div>
</div>

<!-- 事件历史 -->
<div class="card">
  <div class="card-header">
    <div class="card-title">状态历史（最近 50 条）</div>
    <span style="font-size:12px;color:var(--text-2);">近 30 天统计</span>
  </div>
  <div style="overflow-x:auto">
    <table class="data-table">
      <thead>
        <tr>
          <th>时间</th>
          <th>从</th>
          <th>到</th>
          <th>原因</th>
        </tr>
      </thead>
      <tbody>
        <% if (recentEvents.length === 0) { %>
          <tr><td colspan="4" style="text-align:center;padding:32px;color:var(--text-2);">暂无事件记录（启用通知后开始记录）</td></tr>
        <% } %>
        <% recentEvents.forEach(function(ev) { %>
          <tr>
            <td class="text-muted monospace" style="font-size:12px;white-space:nowrap"><%= ev.created_at %></td>
            <td class="text-muted" style="font-size:12px;"><%= ev.from_status || '--' %></td>
            <td style="font-size:12px;">
              <%
                const sc = {
                  running: '#34d399', stalled: '#fbbf24', restarting: '#60a5fa',
                  error: '#f87171', source_retrying: '#fbbf24'
                };
              %>
              <span style="color:<%= sc[ev.to_status] || 'var(--text-1)' %>"><%= ev.to_status %></span>
            </td>
            <td class="text-muted" style="font-size:12px;max-width:300px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;" title="<%= ev.reason || '' %>"><%= ev.reason || '--' %></td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>
</div>
```

<!-- tasks.ejs 任务列表行 — 添加「详情」链接 -->
在任务列表 `<tr>` 中，`<td>` 最后一列（YouTube 端之后）添加：
```html
<td><a href="/tasks/<%= t.id %>" class="link" style="font-size:12px;">详情</a></td>
```
同时在 `<thead><tr>` 中添加对应的 `<th>详情</th>`。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: routes/tasks.js — 添加 GET /:id 路由</name>
  <files>routes/tasks.js</files>
  <action>
    在 `router.get('/', (req, res) => renderTasks(res, req, 200));` 之后，
    `router.post('/', ...)` 之前，添加任务详情路由：
    
    ```js
    router.get('/:id', (req, res) => {
      const taskId = parseInt(req.params.id, 10);
      if (!taskId || isNaN(taskId)) return res.status(400).send('无效任务 ID');
    
      const task = db.prepare(`
        SELECT t.*, v.name as vps_name
        FROM tasks t
        LEFT JOIN vps v ON t.vps_id = v.id
        WHERE t.id = ? AND t.user_id = ?
      `).get(taskId, req.session.userId);
    
      if (!task) return res.status(404).send('任务不存在或无权访问');
    
      const since = new Date(Date.now() - 30 * 24 * 60 * 60 * 1000).toISOString().replace('T', ' ').slice(0, 19);
    
      const statRow = db.prepare(`
        SELECT
          COUNT(CASE WHEN to_status='stalled' THEN 1 END) as stall_count,
          COUNT(CASE WHEN to_status='restarting' THEN 1 END) as restart_count,
          COUNT(CASE WHEN to_status='error' THEN 1 END) as error_count,
          COUNT(CASE WHEN to_status='running' AND from_status IN ('stalled','restarting','source_retrying') THEN 1 END) as recovered_count
        FROM task_events
        WHERE task_id = ? AND created_at >= ?
      `).get(taskId, since);
    
      const recentEvents = db.prepare(`
        SELECT id, from_status, to_status, reason, created_at
        FROM task_events
        WHERE task_id = ?
        ORDER BY created_at DESC
        LIMIT 50
      `).all(taskId);
    
      res.render('task-detail', {
        title: `任务详情 — ${task.name || task.id}`,
        currentPath: '/tasks',
        task,
        stats: statRow || { stall_count: 0, restart_count: 0, error_count: 0, recovered_count: 0 },
        recentEvents,
      });
    });
    ```
  </action>
  <verify>node -e "require('./routes/tasks'); console.log('tasks route OK')" 2>&1</verify>
  <done>
    1. `grep -n "router.get.*/:id\|task_events\|task-detail" routes/tasks.js` 有输出
    2. `node -e "require('./routes/tasks')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 2: 创建 views/task-detail.ejs</name>
  <files>views/task-detail.ejs</files>
  <action>
    创建新文件 `views/task-detail.ejs`：
    
    ```html
    <div class="page-header">
      <div>
        <div class="page-title"><%= task.name || ('任务 #' + task.id) %></div>
        <div class="page-subtitle">
          <a href="/tasks" class="link">任务管理</a> / 详情
        </div>
      </div>
      <a href="/tasks" class="btn btn-secondary">返回列表</a>
    </div>
    
    <!-- 统计卡片 -->
    <div class="section" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));gap:16px;margin-bottom:24px;">
      <div class="stat-card">
        <div class="stat-label" style="margin-bottom:8px;">掉线次数（近30天）</div>
        <div class="stat-value" style="color:<%= stats.stall_count > 0 ? '#fbbf24' : 'var(--text-1)' %>"><%= stats.stall_count %></div>
      </div>
      <div class="stat-card">
        <div class="stat-label" style="margin-bottom:8px;">自动重启（近30天）</div>
        <div class="stat-value" style="color:<%= stats.restart_count > 0 ? '#60a5fa' : 'var(--text-1)' %>"><%= stats.restart_count %></div>
      </div>
      <div class="stat-card">
        <div class="stat-label" style="margin-bottom:8px;">进入错误（近30天）</div>
        <div class="stat-value" style="color:<%= stats.error_count > 0 ? '#f87171' : 'var(--text-1)' %>"><%= stats.error_count %></div>
      </div>
      <div class="stat-card">
        <div class="stat-label" style="margin-bottom:8px;">成功恢复（近30天）</div>
        <div class="stat-value" style="color:<%= stats.recovered_count > 0 ? '#34d399' : 'var(--text-1)' %>"><%= stats.recovered_count %></div>
      </div>
    </div>
    
    <!-- 任务基本信息 -->
    <div class="card" style="margin-bottom:24px;">
      <div class="card-header"><div class="card-title">基本信息</div></div>
      <div class="modal-body">
        <div style="display:grid;grid-template-columns:120px 1fr;gap:8px 16px;font-size:14px;">
          <span style="color:var(--text-2);">状态</span><span><%= task.status %></span>
          <span style="color:var(--text-2);">VPS</span><span><%= task.vps_name || '--' %></span>
          <span style="color:var(--text-2);">平台</span><span><%= task.platform || '--' %></span>
          <span style="color:var(--text-2);">来源</span><span class="monospace" style="font-size:12px;word-break:break-all;"><%= task.source_url %></span>
          <span style="color:var(--text-2);">启动时间</span><span><%= task.started_at || '--' %></span>
          <span style="color:var(--text-2);">最后活跃</span><span><%= task.last_active_at || '--' %></span>
          <span style="color:var(--text-2);">自动重启</span><span><%= task.auto_restart ? '已启用' : '关闭' %></span>
        </div>
      </div>
    </div>
    
    <!-- 事件历史 -->
    <div class="card">
      <div class="card-header">
        <div class="card-title">状态历史（最近 50 条）</div>
        <span style="font-size:12px;color:var(--text-2);">近 30 天统计</span>
      </div>
      <div style="overflow-x:auto">
        <table class="data-table">
          <thead>
            <tr>
              <th>时间</th>
              <th>从</th>
              <th>到</th>
              <th>原因</th>
            </tr>
          </thead>
          <tbody>
            <% if (recentEvents.length === 0) { %>
              <tr><td colspan="4" style="text-align:center;padding:32px;color:var(--text-2);">暂无事件记录（任务运行后自动记录状态变更）</td></tr>
            <% } %>
            <% recentEvents.forEach(function(ev) { %>
              <%
                const sc = {
                  running: '#34d399', stalled: '#fbbf24', restarting: '#60a5fa',
                  error: '#f87171', source_retrying: '#fbbf24'
                };
              %>
              <tr>
                <td class="text-muted monospace" style="font-size:12px;white-space:nowrap"><%= ev.created_at %></td>
                <td class="text-muted" style="font-size:12px;"><%= ev.from_status || '--' %></td>
                <td style="font-size:12px;">
                  <span style="color:<%= sc[ev.to_status] || 'var(--text-1)' %>"><%= ev.to_status %></span>
                </td>
                <td class="text-muted" style="font-size:12px;max-width:300px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;" title="<%= ev.reason || '' %>"><%= ev.reason || '--' %></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      </div>
    </div>
    ```
  </action>
  <verify>node -e "const fs=require('fs'); const c=fs.readFileSync('./views/task-detail.ejs','utf8'); console.log(c.includes('recentEvents') && c.includes('stall_count') ? 'OK' : 'FAIL')"</verify>
  <done>
    1. `views/task-detail.ejs` 文件存在
    2. `grep -n "recentEvents\|stall_count\|task_events" views/task-detail.ejs` 有输出（或 stall_count 在 stats 对象中）
  </done>
</task>

<task type="auto">
  <name>Task 3: views/tasks.ejs — 添加「详情」入口链接</name>
  <files>views/tasks.ejs</files>
  <action>
    在 `views/tasks.ejs` 任务列表表格中：
    
    **步骤 1**：在 `<thead><tr>` 的最后一个 `<th>` 之后添加：
    ```html
    <th></th>
    ```
    
    **步骤 2**：在任务列表 `<tr>` 的最后一个 `<td>` 之后添加：
    ```html
    <td><a href="/tasks/<%= t.id %>" class="link" style="font-size:12px;">详情</a></td>
    ```
    
    先用 Read 工具查看 tasks.ejs 的任务表格结构，确认 `<thead>` 和 `<tr>` 的实际位置，再精确修改。
  </action>
  <verify>node -e "const fs=require('fs'); const c=fs.readFileSync('./views/tasks.ejs','utf8'); console.log(c.includes('/tasks/') || c.includes('task-detail') ? 'OK' : 'FAIL')"</verify>
  <done>
    1. `grep -n "tasks.*t.id\|/tasks/" views/tasks.ejs` 有输出（详情链接）
  </done>
</task>

<task type="auto">
  <name>Task 4: 端到端验证</name>
  <files>routes/tasks.js, views/task-detail.ejs, views/tasks.ejs</files>
  <action>
    1. `node -e "require('./routes/tasks'); console.log('tasks route OK')"`
    2. `grep -n "router.get.*/:id\|task_events" routes/tasks.js`
    3. `node -e "const fs=require('fs'); console.log(fs.existsSync('./views/task-detail.ejs') ? 'view OK' : 'FAIL')"`
    4. `grep -c "recentEvents\|stall_count" views/task-detail.ejs`
  </action>
  <verify>node -e "require('./routes/tasks'); console.log('OK'); process.exit(0)"</verify>
  <done>
    所有验证通过，路由加载成功，视图文件存在。
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('./routes/tasks')"` — 无报错
2. `grep -n "task_events" routes/tasks.js` — 有输出
3. `ls views/task-detail.ejs` — 文件存在
4. `grep -c "recentEvents" views/task-detail.ejs` — >= 2
</verification>

<success_criteria>
- [ ] GET /tasks/:id 路由存在，查询 task_events 计算 30 天统计
- [ ] 路由返回 stall_count、restart_count、error_count、recovered_count 统计
- [ ] views/task-detail.ejs 存在，显示 4 个统计卡片和事件历史表格
- [ ] views/tasks.ejs 任务行有链接到 /tasks/:id 的「详情」入口
- [ ] 模块加载无报错
</success_criteria>

<output>
After completion, create `.planning/phases/04-observability-dashboard/04-03-SUMMARY.md` with:
- GET /:id 路由行号
- task-detail.ejs 行数
- tasks.ejs 详情链接行号
</output>
