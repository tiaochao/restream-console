---
phase: 05-code-quality-errors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/log-error.js
  - services/task-manager.js
  - services/youtube-monitor.js
  - scripts/check.js
autonomous: true
requirements: [QUA-01, QUA-02]

must_haves:
  truths:
    - "utils/log-error.js 导出 logError(context, err) 函数"
    - "logError 输出格式：[ERROR][context] message + 前两行堆栈"
    - "services/task-manager.js 导入 logError，checkHealth 外层 catch 改为 logError"
    - "task-manager.js 的 5 处 catch (_) {} 均已替换为 logError 调用"
    - "services/youtube-monitor.js 导入 logError，2 处 catch (_) {} 改为 logError"
    - "services/youtube-monitor.js 导出 classifyApiError"
    - "scripts/check.js 新增断言：task-manager.js 包含 logError 导入"
    - "node -e \"require('./services/task-manager')\" 无报错"
    - "node -e \"require('./services/youtube-monitor')\" 无报错"
  artifacts:
    - path: "utils/log-error.js"
      provides: "logError(context, err) — 结构化错误日志"
    - path: "services/task-manager.js"
      provides: "checkHealth 外层 catch + 5 处 bare catch → logError"
    - path: "services/youtube-monitor.js"
      provides: "2 处 bare catch → logError，classifyApiError 导出"
---

<objective>
创建 `utils/log-error.js` 并将关键服务文件中的静默 `catch (_) {}` 替换为结构化日志调用。

**重点修复（CONCERNS #16）：**
- `checkHealth()` 外层调用（第 965 行）`.catch(() => {})` 改为 `.catch(err => logError('checkHealth', err))`

**替换位置（task-manager.js 5 处）：**
- 第 73 行：URL 解析失败 → `logError('sourceRecordLabel', err)`
- 第 86 行：DB 查询失败 → `logError('channelRecordLabel', err)`
- 第 516 行：平台直播状态检测失败 → `logError('startTaskQueued/liveCheck', err)`
- 第 534 行：抖音流地址解析失败 → `logError('startTaskQueued/douyinResolve', err)`
- 第 1007 行：checkAndStartIfLive 失败 → `logError('checkAndStartIfLive', err)`

**替换位置（youtube-monitor.js 2 处）：**
- 第 162 行：extractYouTubeVideoId URL 解析失败 → `logError('extractVideoId', err)`
- 第 178 行：extractYouTubeChannelRef URL 解析失败 → `logError('extractChannelRef', err)`

Purpose: 让编程错误有日志可查，消除静默失败。
Output: utils/log-error.js（新建）+ 修改后的 task-manager.js 和 youtube-monitor.js。
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- utils/log-error.js 完整内容 -->
```js
function logError(context, err) {
  const msg = err instanceof Error ? err.message : String(err);
  const lines = (err instanceof Error && err.stack) ? err.stack.split('\n').slice(1, 3) : [];
  console.error(`[ERROR][${context}] ${msg}${lines.length ? '\n' + lines.join('\n') : ''}`);
}

module.exports = { logError };
```

<!-- task-manager.js 顶部 require 块（在 const { writeEvent } = require('../db'); 之后）-->
```js
const { logError } = require('../utils/log-error');
```

<!-- 第 73 行（sourceRecordLabel URL 解析）当前 -->
```js
  } catch (_) {}
  return fallback;
```
改为：
```js
  } catch (err) { logError('sourceRecordLabel', err); }
  return fallback;
```

<!-- 第 86 行（channelRecordLabel DB 查询）当前 -->
```js
  } catch (_) {
    return '';
  }
```
改为：
```js
  } catch (err) {
    logError('channelRecordLabel', err);
    return '';
  }
```

<!-- 第 516 行（startTaskQueued 平台直播检测）当前 -->
```js
    } catch (_) {}
```
改为：
```js
    } catch (err) { logError('startTaskQueued/liveCheck', err); }
```

<!-- 第 534 行（startTaskQueued 抖音流地址解析）当前 -->
```js
      } catch (_) {}
```
改为：
```js
      } catch (err) { logError('startTaskQueued/douyinResolve', err); }
```

<!-- 第 1007 行（checkAndStartIfLive 整体）当前 -->
```js
  } catch (_) {}
```
改为：
```js
  } catch (err) { logError('checkAndStartIfLive', err); }
```

<!-- 第 965 行（checkHealth 外层调用，CONCERNS #16 核心修复）当前 -->
```js
    await processInBatches(active, 3, task => checkHealth(task).catch(() => {}));
```
改为：
```js
    await processInBatches(active, 3, task => checkHealth(task).catch(err => logError('checkHealth', err)));
```

<!-- youtube-monitor.js 顶部导入（在 const { getSetting, setSetting } = require('../db'); 之后）-->
```js
const { logError } = require('../utils/log-error');
```
注意：先检查 youtube-monitor.js 顶部是否已有 setSetting 导入，确认导入位置。

<!-- 第 162 行（extractYouTubeVideoId URL 解析）当前 -->
```js
  } catch (_) {}
  return '';
```
改为：
```js
  } catch (err) { logError('extractVideoId', err); }
  return '';
```

<!-- 第 178 行（extractYouTubeChannelRef URL 解析）当前 -->
```js
  } catch (_) {}
  return {};
```
改为：
```js
  } catch (err) { logError('extractChannelRef', err); }
  return {};
```

<!-- youtube-monitor.js module.exports — 追加 classifyApiError -->
当前 exports（第 422 行附近）包含 startMonitor, scanOnce, checkTask 等，追加：
```js
  classifyApiError,
```

<!-- scripts/check.js — 在最后的 console.log 之前追加断言 -->
```js
assertContains(healthSource, "require('../utils/log-error')", 'task-manager 导入 logError 工具');
assertContains(healthSource, "logError('checkHealth'", 'checkHealth 外层错误日志（CONCERNS #16）");
const youtubeMonitorSource = fs.readFileSync(path.join(root, 'services/youtube-monitor.js'), 'utf8');
assertContains(youtubeMonitorSource, 'classifyApiError,', 'youtube-monitor 导出 classifyApiError');
```
注意：check.js 中已有 `const youtubeSource = ...`，改用 `youtubeSource` 变量名避免重复声明。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 utils/log-error.js</name>
  <files>utils/log-error.js</files>
  <action>
    创建新文件 `utils/log-error.js`：
    ```js
    function logError(context, err) {
      const msg = err instanceof Error ? err.message : String(err);
      const lines = (err instanceof Error && err.stack) ? err.stack.split('\n').slice(1, 3) : [];
      console.error(`[ERROR][${context}] ${msg}${lines.length ? '\n' + lines.join('\n') : ''}`);
    }
    
    module.exports = { logError };
    ```
  </action>
  <verify>node -e "const { logError } = require('./utils/log-error'); logError('test', new Error('hello')); console.log('OK')" 2>&1</verify>
  <done>
    1. `utils/log-error.js` 存在
    2. `node -e "require('./utils/log-error')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 2: task-manager.js — 导入 logError + 修复 6 处 catch</name>
  <files>services/task-manager.js</files>
  <action>
    先 Read 文件确认行号，然后：
    
    **步骤 1**：在 `const { writeEvent } = require('../db');` 之后追加：
    ```js
    const { logError } = require('../utils/log-error');
    ```
    
    **步骤 2**：找到并替换 5 处 `catch (_) {}` + 1 处外层 `.catch(() => {})` ：
    
    - `sourceRecordLabel` 函数末：`} catch (_) {}` → `} catch (err) { logError('sourceRecordLabel', err); }`
    - `channelRecordLabel` 函数末：`} catch (_) { return ''; }` → `} catch (err) { logError('channelRecordLabel', err); return ''; }`
    - 平台直播检测末：`} catch (_) {}` → `} catch (err) { logError('startTaskQueued/liveCheck', err); }`
    - 抖音流解析末：`} catch (_) {}` → `} catch (err) { logError('startTaskQueued/douyinResolve', err); }`
    - checkAndStartIfLive 末：`} catch (_) {}` → `} catch (err) { logError('checkAndStartIfLive', err); }`
    - checkHealth 外层调用：`.catch(() => {})` → `.catch(err => logError('checkHealth', err))`
    
    注意：用 grep 找到每处确切行号后再精确修改，避免误改其他 `.catch(() => {})` 行。
    只修改 `task => checkHealth(task).catch(() => {})` 这一行，不改其他 Promise chain 的 .catch。
  </action>
  <verify>node -e "require('./services/task-manager'); console.log('OK')" 2>&1</verify>
  <done>
    1. `grep -n "logError" services/task-manager.js | wc -l` 输出 >= 6
    2. `grep -c "catch (_) {}" services/task-manager.js` 输出 0
    3. `node -e "require('./services/task-manager')"` 无报错
  </done>
</task>

<task type="auto">
  <name>Task 3: youtube-monitor.js — 导入 logError + 修复 2 处 catch + 导出 classifyApiError</name>
  <files>services/youtube-monitor.js</files>
  <action>
    先 Read 文件确认顶部结构和行号，然后：
    
    **步骤 1**：在顶部 require 块末尾追加：
    ```js
    const { logError } = require('../utils/log-error');
    ```
    
    **步骤 2**：找到 `extractYouTubeVideoId` 函数内的 `} catch (_) {}` → 改为：
    ```js
      } catch (err) { logError('extractVideoId', err); }
    ```
    
    **步骤 3**：找到 `extractYouTubeChannelRef` 函数内的 `} catch (_) {}` → 改为：
    ```js
    } catch (err) { logError('extractChannelRef', err); }
    ```
    
    **步骤 4**：在 `module.exports = {` 块中，在 `startMonitor,` 之后追加：
    ```js
      classifyApiError,
    ```
  </action>
  <verify>node -e "const ym = require('./services/youtube-monitor'); console.log(typeof ym.classifyApiError === 'function' ? 'OK' : 'FAIL')" 2>&1</verify>
  <done>
    1. `grep -n "logError\|classifyApiError" services/youtube-monitor.js | head -10` 有输出
    2. `grep -c "catch (_) {}" services/youtube-monitor.js` 输出 0
    3. `node -e "const ym = require('./services/youtube-monitor'); console.log(typeof ym.classifyApiError)"` 输出 function
  </done>
</task>

<task type="auto">
  <name>Task 4: scripts/check.js — 新增 logError 和 classifyApiError 断言</name>
  <files>scripts/check.js</files>
  <action>
    Read `scripts/check.js` 找到最后的 `console.log(...)` 行，在其**之前**插入：
    ```js
    assertContains(healthSource, "require('../utils/log-error')", 'task-manager 导入 logError 工具');
    assertContains(healthSource, "logError('checkHealth'", 'checkHealth 外层错误日志（CONCERNS #16）');
    assertContains(youtubeSource, 'classifyApiError,', 'youtube-monitor 导出 classifyApiError');
    ```
    
    注意：check.js 中已有变量 `healthSource`（task-manager.js 内容）和 `youtubeSource`（youtube-monitor.js 内容），
    直接复用这些变量，不重复 readFileSync。
  </action>
  <verify>node scripts/check.js 2>&1 | tail -5</verify>
  <done>
    1. `node scripts/check.js` 无错误，输出 "OK: checked..."
    2. `grep -n "logError.*CONCERNS\|classifyApiError" scripts/check.js` 有输出
  </done>
</task>

<task type="auto">
  <name>Task 5: 端到端验证</name>
  <files>utils/log-error.js, services/task-manager.js, services/youtube-monitor.js</files>
  <action>
    1. `node -e "const {logError}=require('./utils/log-error');logError('test',new Error('hello'));console.log('logError OK')"`
    2. `node -e "require('./services/task-manager');console.log('task-manager OK')"`
    3. `node -e "const ym=require('./services/youtube-monitor');console.log(typeof ym.classifyApiError,'youtube-monitor OK')"`
    4. `node scripts/check.js` — 输出 OK
    5. `grep -c "catch (_) {}" services/task-manager.js services/youtube-monitor.js` — 输出 0
  </action>
  <verify>node scripts/check.js 2>&1 | tail -3</verify>
  <done>
    所有验证通过，check.js 输出 OK，无 bare catch 残留。
  </done>
</task>

</tasks>

<verification>
1. `node -e "require('./utils/log-error')"` — 无报错
2. `node -e "require('./services/task-manager')"` — 无报错
3. `node -e "const ym=require('./services/youtube-monitor'); console.log(typeof ym.classifyApiError)"` — function
4. `node scripts/check.js` — 无报错，输出 OK
5. `grep -c "catch (_) {}" services/task-manager.js` — 0
</verification>

<success_criteria>
- [ ] utils/log-error.js 存在，导出 logError(context, err)
- [ ] task-manager.js 导入 logError，checkHealth 外层 .catch(() => {}) 已修复
- [ ] task-manager.js 中 0 处 catch (_) {} 残留
- [ ] youtube-monitor.js 导入 logError，导出 classifyApiError，0 处 catch (_) {} 残留
- [ ] scripts/check.js 通过（含新断言）
</success_criteria>

<output>
After completion, create `.planning/phases/05-code-quality-errors/05-01-SUMMARY.md` with:
- logError 调用总数
- 修复的 catch 位置列表
- classifyApiError 导出行号
</output>
