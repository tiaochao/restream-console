---
phase: 06-code-quality-refactor
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - services/ffmpeg-args.js
  - services/task-manager.js
autonomous: true
requirements: [QUA-03]

must_haves:
  truths:
    - "services/ffmpeg-args.js 存在，导出 buildCommand, recordLabelForTask, isYoutubeTarget, remoteDependencyInstallCommand, autoRecordingCompatPath, MEDIA_LIBRARY_DIR, LEGACY_RECORD_DIR, AUTO_RECORDING_PREFIX"
    - "services/task-manager.js 导入 ffmpeg-args.js，不再本地定义 buildCommand 和 label 相关函数"
    - "task-manager.js 的 module.exports 仍包含 _buildCommand（通过 ffmpeg-args re-export）"
    - "node -e \"require('./services/task-manager')\" 无报错"
    - "npm test 26/26 通过"
  artifacts:
    - path: "services/ffmpeg-args.js"
      provides: "FFmpeg 命令构建 + 标签工具函数 + 共享常量"
---

<objective>
将 task-manager.js 中的 FFmpeg 命令生成逻辑（buildCommand + label 辅助函数 + ffmpeg 参数函数）
抽取到独立的 `services/ffmpeg-args.js` 模块。

这是最大的一次提取，预计从 task-manager.js 中删除约 335 行。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 创建 services/ffmpeg-args.js</name>
  <files>services/ffmpeg-args.js</files>

  <read_first>
  - services/task-manager.js（精确读取以下区域：
    - 行 1-20：imports + constants
    - 行 44-110：dqEsc, shSingleQuote, normalizeRecordLabel, sourceRecordLabel, channelRecordLabel, recordLabelForTask, autoRecordingCompatName, autoRecordingCompatPath
    - 行 162-207：isYoutubeTarget, ffmpegOutputArgs, ffmpegRecordArgs, ffmpegHttpInputArgs, remoteDependencyInstallCommand
    - 行 208-477：buildCommand 完整实现）
  </read_first>

  <action>
  创建 `services/ffmpeg-args.js`，内容包含（按此顺序）：

  **1. 顶部 imports：**
  ```js
  const db = require('../db');
  const { getSetting } = require('../db');
  const { decrypt } = require('./crypto');
  const { logError } = require('../utils/log-error');
  const { dqEsc, shSingleQuote } = require('../utils/shell-escape');
  ```

  **2. 常量（从 task-manager.js 第 12-20 行复制）：**
  ```js
  const MEDIA_LIBRARY_DIR = '/root/restream_uploads';
  const LEGACY_RECORD_DIR = '/root/restream_recordings';
  const AUTO_RECORDING_PREFIX = '录播';
  ```

  **3. 所有 label 相关函数（从 task-manager.js 第 56-110 行复制）：**
  - normalizeRecordLabel(value, fallback)
  - sourceRecordLabel(sourceUrl, fallback)
  - channelRecordLabel(task)
  - recordLabelForTask(task)
  - autoRecordingCompatName(taskId)
  - autoRecordingCompatPath(taskId)

  **4. FFmpeg 辅助函数（从 task-manager.js 第 162-186 行复制）：**
  - isYoutubeTarget(task)
  - ffmpegOutputArgs(task)
  - ffmpegRecordArgs()
  - ffmpegHttpInputArgs(headers)
  - remoteDependencyInstallCommand()

  **5. buildCommand 函数（从 task-manager.js 第 208-477 行复制）**
     注意：buildCommand 内部引用了 dqEsc, shSingleQuote（现在从 shell-escape import），
     ffmpegOutputArgs, ffmpegRecordArgs, ffmpegHttpInputArgs, isYoutubeTarget, recordLabelForTask,
     autoRecordingCompatPath, MEDIA_LIBRARY_DIR, AUTO_RECORDING_PREFIX。
     这些都已在本文件内定义，无需额外处理。

  **6. module.exports：**
  ```js
  module.exports = {
    buildCommand,
    recordLabelForTask,
    isYoutubeTarget,
    remoteDependencyInstallCommand,
    autoRecordingCompatPath,
    autoRecordingCompatName,
    MEDIA_LIBRARY_DIR,
    LEGACY_RECORD_DIR,
    AUTO_RECORDING_PREFIX,
  };
  ```

  重要：直接从 task-manager.js 复制这些函数的完整实现，不要简化或修改逻辑。
  buildCommand 函数有约 270 行，必须完整复制。
  </action>

  <acceptance_criteria>
  - services/ffmpeg-args.js 文件存在
  - `node -e "const f=require('./services/ffmpeg-args');console.log(typeof f.buildCommand, typeof f.recordLabelForTask, f.MEDIA_LIBRARY_DIR)"` 输出 "function function /root/restream_uploads"
  - `grep -c "module.exports" services/ffmpeg-args.js` 输出 1
  - `wc -l services/ffmpeg-args.js`（或等价命令）显示行数大于 300（buildCommand 本身就 270 行）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 task-manager.js — 移除已抽取代码，改用 ffmpeg-args import</name>
  <files>services/task-manager.js</files>

  <read_first>
  - services/task-manager.js（全文，确认行号和 import 区域）
  </read_first>

  <action>
  **步骤 1：** 在 task-manager.js 顶部 require 块（第 1-10 行之后）添加：
  ```js
  const {
    buildCommand,
    recordLabelForTask,
    isYoutubeTarget,
    remoteDependencyInstallCommand,
    autoRecordingCompatPath,
    MEDIA_LIBRARY_DIR,
    LEGACY_RECORD_DIR,
    AUTO_RECORDING_PREFIX,
  } = require('./ffmpeg-args');
  const { dqEsc, shSingleQuote } = require('../utils/shell-escape');
  ```
  注意：dqEsc/shSingleQuote 在 syncDouyinHelper 和 syncAutoRecordingMediaFile 中仍被使用（这些函数在 06-03 才移走），所以本计划中需要保留这个 import。

  **步骤 2：** 删除以下代码块（已移至 ffmpeg-args.js）：

  2a. 常量定义（约第 12-20 行）：
  ```js
  const MEDIA_LIBRARY_DIR = '/root/restream_uploads';
  const LEGACY_RECORD_DIR = '/root/restream_recordings';
  const AUTO_RECORDING_PREFIX = '录播';
  ```

  2b. 本地 dqEsc 和 shSingleQuote 函数定义（约第 44-54 行）：
  ```js
  function dqEsc(s) { ... }
  function shSingleQuote(s) { ... }
  ```

  2c. label 相关函数（约第 56-110 行）：
  - normalizeRecordLabel
  - sourceRecordLabel
  - channelRecordLabel
  - recordLabelForTask
  - autoRecordingCompatName
  - autoRecordingCompatPath

  2d. FFmpeg 辅助函数（约第 162-186 行）：
  - isYoutubeTarget
  - ffmpegOutputArgs
  - ffmpegRecordArgs
  - ffmpegHttpInputArgs
  - remoteDependencyInstallCommand

  2e. buildCommand 函数（约第 208-477 行，共 270 行）

  **步骤 3：** 更新 module.exports，确保 `_buildCommand` 仍指向 buildCommand（来自 ffmpeg-args）：
  当前 module.exports 末尾有：`_buildCommand: buildCommand`
  这行不需要修改，因为 buildCommand 现在是从 ffmpeg-args 导入的，名称相同。

  使用精确字符串匹配删除各个区块，每次用 Edit 工具的 old_string 匹配确切代码。
  </action>

  <acceptance_criteria>
  - `node -e "require('./services/task-manager');console.log('OK')"` 无报错
  - `node -e "const tm=require('./services/task-manager');console.log(typeof tm._buildCommand)"` 输出 "function"
  - `grep -c "^function buildCommand\|^function normalizeRecordLabel\|^function ffmpegOutputArgs" services/task-manager.js` 输出 0（这些函数已移走）
  - `grep -c "require.*ffmpeg-args" services/task-manager.js` 输出 1
  - task-manager.js 行数小于 700（从 990 行减少约 335 行）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 运行测试验证重构不破坏功能</name>
  <files>无（只运行验证命令）</files>

  <read_first>无需额外读取</read_first>

  <action>
  1. `node -e "require('./services/ffmpeg-args');console.log('ffmpeg-args OK')"`
  2. `node -e "require('./services/task-manager');console.log('task-manager OK')"`
  3. `npm test` — 期望 26/26 通过
  </action>

  <acceptance_criteria>
  - npm test 输出 "26 passed, 26 total"（或更多，不允许有失败）
  - task-manager.js 和 ffmpeg-args.js require 无报错
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `node -e "require('./services/ffmpeg-args')"` — 无报错
2. `node -e "const tm=require('./services/task-manager');console.log(typeof tm._buildCommand)"` — function
3. `grep -c "^function buildCommand" services/task-manager.js` — 0
4. `npm test` — 26 passed
</verification>

<success_criteria>
- [ ] services/ffmpeg-args.js 存在，导出 buildCommand, recordLabelForTask, isYoutubeTarget 等
- [ ] task-manager.js 导入 ffmpeg-args，_buildCommand re-export 正常
- [ ] task-manager.js 中不再有 buildCommand/normalizeRecordLabel/ffmpegOutputArgs 本地定义
- [ ] npm test 26/26 PASS
</success_criteria>
