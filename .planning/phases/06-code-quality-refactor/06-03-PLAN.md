---
phase: 06-code-quality-refactor
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - services/task-ssh.js
  - services/task-manager.js
autonomous: true
requirements: [QUA-03]

must_haves:
  truths:
    - "services/task-ssh.js 存在，导出 syncDouyinHelper, ensureRemoteRuntime, syncAutoRecordingMediaFile"
    - "task-manager.js 导入 task-ssh.js，不再本地定义这三个函数"
    - "task-manager.js 不再有 shell-escape 的本地 import（shSingleQuote 已随 syncDouyinHelper 移走）"
    - "node -e \"require('./services/task-manager')\" 无报错"
    - "npm test 仍全部通过"
  artifacts:
    - path: "services/task-ssh.js"
      provides: "SSH 相关操作：同步 helper 脚本、确保远端运行时、同步录播媒体文件"
---

<objective>
将 task-manager.js 中负责 SSH 下发和文件同步的三个函数提取到 `services/task-ssh.js`。

预计从 task-manager.js 中删除约 65 行：
- syncAutoRecordingMediaFile (lines 112-161, 50 行)
- syncDouyinHelper (lines 188-197, 10 行)
- ensureRemoteRuntime (lines 199-202, 4 行)

同时移除不再需要的 shell-escape import（因为这两个函数是唯一在 task-manager.js 中使用 shSingleQuote 的地方）。
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 创建 services/task-ssh.js</name>
  <files>services/task-ssh.js</files>

  <read_first>
  - services/task-manager.js（精确读取：
    - 行 112-161：syncAutoRecordingMediaFile 完整实现
    - 行 188-202：syncDouyinHelper + ensureRemoteRuntime 完整实现）
  </read_first>

  <action>
  创建 `services/task-ssh.js`，内容如下：

  **1. 顶部 imports：**
  ```js
  const path = require('path');
  const fs = require('fs');
  const db = require('../db');
  const sshService = require('./ssh');
  const { shSingleQuote } = require('../utils/shell-escape');
  const {
    MEDIA_LIBRARY_DIR,
    LEGACY_RECORD_DIR,
    AUTO_RECORDING_PREFIX,
    autoRecordingCompatName,
    autoRecordingCompatPath,
    remoteDependencyInstallCommand,
  } = require('./ffmpeg-args');
  ```

  **2. syncDouyinHelper 函数（从 task-manager.js 第 188-197 行复制）：**
  ```js
  async function syncDouyinHelper(vpsId, userId) {
    const scriptPath = path.join(__dirname, '..', 'check_douyin.py');
    const script = fs.readFileSync(scriptPath, 'utf8');
    const scriptB64 = Buffer.from(script).toString('base64');
    await sshService.exec(vpsId, [
      'mkdir -p /opt/restream-console',
      `printf %s ${shSingleQuote(scriptB64)} | base64 -d > /opt/restream-console/check_douyin.py`,
      'chmod +x /opt/restream-console/check_douyin.py',
    ].join(' && '), userId);
  }
  ```

  **3. ensureRemoteRuntime 函数（从 task-manager.js 第 199-202 行复制）：**
  ```js
  async function ensureRemoteRuntime(vpsId, userId, options = {}) {
    await sshService.exec(vpsId, remoteDependencyInstallCommand(), userId);
    if (options.douyinHelper) await syncDouyinHelper(vpsId, userId);
  }
  ```

  **4. syncAutoRecordingMediaFile 函数（从 task-manager.js 第 112-161 行完整复制）**
  注意：该函数内部使用了 shSingleQuote, MEDIA_LIBRARY_DIR, LEGACY_RECORD_DIR,
  AUTO_RECORDING_PREFIX, autoRecordingCompatName, autoRecordingCompatPath, db, sshService，
  这些都已在本文件的 imports 中引入。

  **5. module.exports：**
  ```js
  module.exports = { syncDouyinHelper, ensureRemoteRuntime, syncAutoRecordingMediaFile };
  ```
  </action>

  <acceptance_criteria>
  - services/task-ssh.js 文件存在
  - `node -e "const t=require('./services/task-ssh');console.log(typeof t.syncDouyinHelper, typeof t.ensureRemoteRuntime, typeof t.syncAutoRecordingMediaFile)"` 输出 "function function function"
  - `grep -c "require.*shell-escape" services/task-ssh.js` 输出 1
  - `grep -c "require.*ffmpeg-args" services/task-ssh.js` 输出 1
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 更新 task-manager.js — 移除 SSH 函数，改用 task-ssh import</name>
  <files>services/task-manager.js</files>

  <read_first>
  - services/task-manager.js（确认当前的 import 区域和需要删除的函数）
  </read_first>

  <action>
  **步骤 1：** 在 task-manager.js 顶部 require 块中，替换或追加（在 ffmpeg-args import 之后）：
  ```js
  const { syncDouyinHelper, ensureRemoteRuntime, syncAutoRecordingMediaFile } = require('./task-ssh');
  ```

  **步骤 2：** 删除 task-manager.js 中以下代码块（已移至 task-ssh.js）：

  2a. syncAutoRecordingMediaFile 函数（约第 112-161 行，50 行）

  2b. syncDouyinHelper 函数（约第 188-197 行）

  2c. ensureRemoteRuntime 函数（约第 199-202 行）

  **步骤 3：** 删除 task-manager.js 中的 shell-escape import（因为 shSingleQuote 随 SSH 函数移走了，task-manager.js 本身不再直接使用 dqEsc/shSingleQuote）：
  删除这一行：
  ```js
  const { dqEsc, shSingleQuote } = require('../utils/shell-escape');
  ```

  **注意：** fs 和 path 的 import 在 task-manager.js 中可能仍被其他地方使用，不要删除。
  检查 task-manager.js 中 `fs.` 和 `path.` 的使用情况，只有确认不再使用时才删除对应 import。
  </action>

  <acceptance_criteria>
  - `node -e "require('./services/task-manager');console.log('OK')"` 无报错
  - `grep -c "require.*task-ssh" services/task-manager.js` 输出 1
  - `grep -c "^async function syncAutoRecordingMediaFile\|^async function syncDouyinHelper\|^async function ensureRemoteRuntime" services/task-manager.js` 输出 0
  - `grep -c "require.*shell-escape" services/task-manager.js` 输出 0（shell-escape 移到 task-ssh 后，task-manager 不再需要它）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 验证重构</name>
  <files>无（只运行验证）</files>

  <read_first>无需额外读取</read_first>

  <action>
  运行验证命令：
  1. `node -e "require('./services/task-ssh');console.log('task-ssh OK')"`
  2. `node -e "require('./services/task-manager');console.log('task-manager OK')"`
  3. 检查 task-manager.js 行数（应小于 650，比 Plan 06-02 后再少约 65 行）
  4. `npm test` — 期望全部通过
  </action>

  <acceptance_criteria>
  - npm test 全部通过（不允许有失败）
  - task-manager.js require 无报错
  - task-ssh.js require 无报错
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `node -e "require('./services/task-ssh')"` — 无报错
2. `node -e "require('./services/task-manager')"` — 无报错
3. `grep -c "^async function syncAutoRecordingMediaFile" services/task-manager.js` — 0
4. `grep -c "require.*shell-escape" services/task-manager.js` — 0
5. `npm test` — 全部通过
</verification>

<success_criteria>
- [ ] services/task-ssh.js 存在，导出三个函数
- [ ] task-manager.js 使用 task-ssh import，移除了三个本地函数定义
- [ ] task-manager.js 移除了 shell-escape import
- [ ] npm test 全部通过
</success_criteria>
