---
phase: 06-code-quality-refactor
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - services/task-state.js
  - services/task-manager.js
autonomous: true
requirements: [QUA-03]

must_haves:
  truths:
    - "services/task-state.js 存在，导出 buildHealthCheckCmd, parseHealthResult, evaluateHealth"
    - "task-manager.js 中的 checkHealth 函数使用 task-state.js 的三个函数，缩减至约 40 行"
    - "task-manager.js 总行数不超过 350 行"
    - "npm test 26/26 通过（或更多，不允许失败）"
    - "node -e \"require('./services/task-manager')\" 无报错"
  artifacts:
    - path: "services/task-state.js"
      provides: "健康状态机：SSH 命令构建、结果解析、状态决策（纯函数，无副作用）"
---

<objective>
将 checkHealth() 函数中的状态机判断逻辑（约 250 行）抽取到 `services/task-state.js`。

checkHealth() 在 task-manager.js 中将缩减为约 40 行的"效果执行器"，
task-state.js 中的纯函数负责决策（无 DB 写入、无 SSH 调用、无通知）。

这是最后一步，完成后 task-manager.js 应降至 ≤350 行。

**API 设计：**

```js
// 构建健康检测 SSH 命令
buildHealthCheckCmd(task) → string

// 解析 SSH 结果的 4 行输出
parseHealthResult(task, sshLines, now, stallTimeout) → {
  procStatus,    // 'alive' | 'dead'
  mtime,         // number
  statusJson,    // { state, source, target, fallback }
  rtmpStatus,    // 'rtmp_connected' | 'no_rtmp' | 'rtmp_unknown'
  stale,         // boolean
  isFallbackActive, isRetryLoop, isSourceOffline, isSourceUnavailable,
  isTargetLost, isExpiredDirectUrl, isFfmpegNoStreamError, isLiveSource,
  isYoutubeRtmpMissing, isTargetStatus, isBlocked, isRtmpError,
  hasHealthyFrameAfterErrors,
}

// 返回效果描述（纯函数，无副作用）
evaluateHealth(task, parsed, { blockLimit }) → {
  action: string,        // 'noOp' | 'recover' | 'stall' | 'setStatus' | 'source_offline' | 'restart' | 'error_stop'
  newStatus: string?,    // null = 不更改 status
  newStallCount: number?,
  newBlockCount: number?,
  clearPid: bool,
  requiresStop: bool,
  requiresRestart: bool,
  notifyType: string?,
  notifyMsg: string?,
  eventToStatus: string?,
  eventReason: string?,
  logMsg: string?,
}
```
</objective>

<tasks>

<task type="auto">
  <name>Task 1: 精读 checkHealth 完整实现（必须步骤）</name>
  <files>services/task-manager.js（行 632-932）</files>

  <read_first>
  - services/task-manager.js 行 632-932（checkHealth 完整实现）
  - services/ffmpeg-args.js（确认 isYoutubeTarget 导出，task-state.js 将需要导入它）
  </read_first>

  <action>
  仔细阅读 checkHealth 函数（行 632-932），理解所有决策分支：
  1. 行 643-648：SSH 复合命令构建（→ buildHealthCheckCmd）
  2. 行 651-710：SSH 结果解析和 boolean 标志推导（→ parseHealthResult）
  3. 行 712-927：12 个决策分支（→ evaluateHealth）
  4. 行 928-931：catch 块

  此步骤确保精确理解代码后再创建 task-state.js。
  </action>

  <acceptance_criteria>
  - 此步骤不产生文件变更，仅为后续步骤做准备
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 创建 services/task-state.js</name>
  <files>services/task-state.js</files>

  <read_first>
  - services/task-manager.js 行 632-932（checkHealth 实现，必须精确复制逻辑）
  - services/ffmpeg-args.js（确认 isYoutubeTarget 函数签名）
  </read_first>

  <action>
  创建 `services/task-state.js`，内容如下：

  **1. imports：**
  ```js
  const { isYoutubeTarget } = require('./ffmpeg-args');
  ```

  **2. buildHealthCheckCmd(task)：**
  从 checkHealth 行 643-648 提取 SSH 命令数组，返回 join 后的字符串。
  该命令由 4 个子命令组成（进程存活检测、log mtime、状态文件读取、RTMP 连接检测），
  用分号连接。精确复制这段命令字符串生成逻辑。

  **3. parseHealthResult(task, sshLines, now, stallTimeout)：**
  从 checkHealth 行 651-710 提取，接受原始 SSH 输出的 split 后数组（sshLines），
  计算并返回所有派生布尔标志：
  - procStatus = sshLines[0]?.trim()
  - mtime = parseInt(sshLines[1]?.trim() || '0')
  - statusJson 从 sshLines[2] JSON.parse（失败时返回 {}）
  - rtmpStatus = sshLines[3]?.trim() || 'rtmp_unknown'
  - stale = mtime > 0 && (now - mtime) > stallTimeout
  - jsState, jsSource, jsTarget, jsFallback（从 statusJson 提取）
  - isFallbackActive, isRetryLoop, isSourceOffline, isSourceUnavailable, isTargetLost, isExpiredDirectUrl, isLiveSource
  - isFfmpegNoStreamError = jsState === 'idle' && procStatus !== 'dead' && mtime > 0 && (now - mtime) < 30
  - expectsRtmp1935 = /^rtmp:\/\//i.test(String(task.rtmp_url || ''))
  - isYoutubeRtmpMissing = isYoutubeTarget(task) && expectsRtmp1935 && procStatus !== 'dead' && rtmpStatus === 'no_rtmp'
  - isTargetStatus = task.status === 'target_lost'
  - isBlocked = false（当前硬编码）
  - isRtmpError = jsTarget === 'lost' && procStatus !== 'dead' && !jsFallback
  - hasHealthyFrameAfterErrors = jsState === 'streaming'

  **4. evaluateHealth(task, parsed, { blockLimit })：**
  纯函数，无副作用，精确映射 checkHealth 行 712-927 的 12 个决策分支，
  返回效果对象。

  效果对象结构：
  ```js
  {
    action,        // 描述性标签（仅用于 task-manager 的 switch）
    newStatus,     // null 或新状态字符串
    newStallCount, // null 或新计数
    newBlockCount, // null 或新计数
    clearPid,      // true = remote_pid 设为 NULL
    requiresStop,  // true = 需调用 stopTask
    requiresRestart, // true = 需调用 startTaskQueued
    notifyType,    // null 或通知类型字符串
    notifyMsg,     // null 或通知消息
    eventToStatus, // null 或事件目标状态
    eventReason,   // null 或事件原因
    logMsg,        // null 或 console.log/warn 消息
  }
  ```

  12 个分支的映射（精确参照原始逻辑，不简化）：

  **分支1（isFfmpegNoStreamError && !hasHealthyFrameAfterErrors）:**
  stallCount++；
  return { action: 'restart', newStallCount: newStallCount, requiresStop: true, requiresRestart: true,
    notifyType: 'task_restarting', notifyMsg: `任务 ${task.name||task.id} 自动重启中`,
    eventToStatus: 'restarting', eventReason: 'auto_restart',
    logMsg: `[health] task ${task.id} ffmpeg output has no streams, restarting...` }

  （若 stallCount < 1 时 return stall 分支 — 查看原始代码确认 if newStallCount >= 1 的判断）

  **分支2（isExpiredDirectUrl && isLiveSource）:**
  stallCount++；
  if isFallbackActive → return { action: 'setRunning', newStatus: 'running', ... }
  else → return { action: 'setStatus', newStatus: 'source_retrying', newStallCount }

  **分支3（isRetryLoop && isLiveSource）:**
  stallCount++；
  return { action: 'setStatus', newStatus: 'source_retrying', newStallCount }

  **分支4（isTargetLost || isTargetStatus）:**
  stallCount++；
  if newStallCount >= 2 || isTargetStatus:
    if task.auto_restart → restart
    else → { requiresStop: true, newStatus: 'target_lost', clearPid: true }
  else → { newStatus: 'target_lost', newStallCount }

  **分支5（isYoutubeRtmpMissing && !isRetryLoop && !isExpiredDirectUrl && !isSourceUnavailable）:**
  stallCount++；
  if newStallCount >= 2:
    if task.auto_restart → restart
    else → stall
  else if task.status !== 'stalled' → stall

  **分支6（isSourceUnavailable && isLiveSource）:**
  stallCount++；
  if isFallbackActive → setRunning
  if !isSourceOffline || newStallCount < 2: 
    if task.status !== 'stalled' → stall
  else → { requiresStop: true, newStatus: 'waiting_live', clearPid: true, newStallCount: 0 }

  **分支7（isBlocked）:**
  blockCount++；
  if blockCount >= blockLimit → { requiresStop: true, newStatus: 'blocked', clearPid: true }

  **分支8（isRtmpError）:**
  stallCount++；
  if newStallCount >= 3:
    if task.auto_restart → restart
    else → { requiresStop: true, newStatus: 'error', clearPid: true, notifyType: 'task_error', eventReason: 'process_died' }
  else if task.status !== 'stalled' → stall

  **分支9（procStatus === 'dead'）:**
  if task.auto_restart → { action: 'restart', requiresRestart: true (无需 stop，进程已死), notifyType: 'task_restarting', eventReason: 'auto_restart' }
  else → { newStatus: 'error', clearPid: true, notifyType: 'task_error', eventReason: 'process_died' }

  **分支10（stale || isRetryLoop）:**
  stallCount++；
  retryThreshold = isRetryLoop && !stale ? 10 : 1
  if newStallCount < retryThreshold:
    if task.status !== 'stalled' → stall
  else if task.auto_restart → { requiresStop: true, requiresRestart: true, ... }
  else → { requiresStop: true, newStatus: 'error', clearPid: true, notifyType: 'task_error', eventReason: 'no_log_update' }

  **分支11（mtime > 0，健康）:**
  return { action: 'recover', newStallCount: 0, newBlockCount: 0 }

  **默认（mtime === 0）:**
  return { action: 'noOp' }

  **5. module.exports：**
  ```js
  module.exports = { buildHealthCheckCmd, parseHealthResult, evaluateHealth };
  ```

  **关键规则：**
  - evaluateHealth 是纯函数，禁止调用 db, sshService, notifier, console 等
  - 所有决策完全基于传入参数
  - 精确复制原始逻辑，不简化或合并分支
  </action>

  <acceptance_criteria>
  - services/task-state.js 文件存在
  - `node -e "const s=require('./services/task-state');console.log(typeof s.buildHealthCheckCmd, typeof s.parseHealthResult, typeof s.evaluateHealth)"` 输出 "function function function"
  - `grep -c "db\." services/task-state.js` 输出 0（纯函数，无 DB 调用）
  - `grep -c "sshService" services/task-state.js` 输出 0（纯函数，无 SSH 调用）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 重构 checkHealth — 使用 task-state.js</name>
  <files>services/task-manager.js</files>

  <read_first>
  - services/task-manager.js（行 1-15 的 imports 区域，行 632-932 的 checkHealth 实现）
  - services/task-state.js（确认三个函数的签名）
  </read_first>

  <action>
  **步骤 1：** 在 task-manager.js 顶部 require 块添加：
  ```js
  const { buildHealthCheckCmd, parseHealthResult, evaluateHealth } = require('./task-state');
  ```

  **步骤 2：** 将 checkHealth 函数（行 632-932，约 300 行）替换为精简版本（约 60 行）：

  ```js
  async function checkHealth(task) {
    if (!task.remote_pid || !task.vps_id) return;

    const stallTimeout = parseInt(getSetting('stall_timeout', task.user_id) || '120');
    const blockLimit   = parseInt(getSetting('block_limit', task.user_id)   || '8');

    try {
      await syncAutoRecordingMediaFile(task).catch(() => {});

      const cmd = buildHealthCheckCmd(task);
      const result = await sshService.exec(task.vps_id, cmd, task.user_id);
      const now = Math.floor(Date.now() / 1000);
      const sshLines = result.stdout.trim().split('\n');
      const parsed = parseHealthResult(task, sshLines, now, stallTimeout);
      const effect = evaluateHealth(task, parsed, { blockLimit });

      if (effect.logMsg) console.log(effect.logMsg);
      if (effect.newStallCount !== null) {
        db.prepare('UPDATE tasks SET stall_count=? WHERE id=?').run(effect.newStallCount, task.id);
      }
      if (effect.newBlockCount !== null) {
        db.prepare('UPDATE tasks SET block_count=? WHERE id=?').run(effect.newBlockCount, task.id);
      }
      if (effect.requiresStop) {
        await stopTask(task.id, task.user_id).catch(() => {});
      }
      if (effect.requiresRestart) {
        db.prepare("UPDATE tasks SET status='restarting' WHERE id=?").run(task.id);
        startTaskQueued(task.id, task.user_id).catch(() => {});
      } else if (effect.action === 'recover') {
        const statusPatch = ['stalled', 'source_retrying'].includes(task.status) ? ", status='running'" : '';
        db.prepare(`UPDATE tasks SET last_active_at=datetime('now'), stall_count=0, block_count=0${statusPatch} WHERE id=?`).run(task.id);
        if ((task.stall_count || 0) > 0 || ['stalled', 'restarting', 'source_retrying'].includes(task.status)) {
          _notify(task, 'task_recovered', `任务 ${task.name || task.id} 已恢复正常`);
          _record(task, task.status, 'running', 'recovered');
        }
      } else if (effect.newStatus) {
        if (effect.clearPid) {
          db.prepare('UPDATE tasks SET status=?, remote_pid=NULL WHERE id=?').run(effect.newStatus, task.id);
        } else if (effect.action === 'setRunning') {
          db.prepare("UPDATE tasks SET status='running', last_active_at=datetime('now') WHERE id=?").run(task.id);
        } else {
          db.prepare('UPDATE tasks SET status=? WHERE id=?').run(effect.newStatus, task.id);
        }
      }
      if (effect.notifyType) {
        _notify(task, effect.notifyType, effect.notifyMsg || `任务 ${task.name || task.id} 状态变更`);
      }
      if (effect.eventReason) {
        _record(task, task.status, effect.eventToStatus || effect.newStatus || task.status, effect.eventReason);
      }
    } catch (err) {
      logError('checkHealth', err);
    }
  }
  ```

  **注意：** 必须确保效果执行器的行为与原始 checkHealth 完全一致。
  仔细比对每个分支，必要时查阅原始 checkHealth 代码进行校对。
  如果某个分支的 log 语句（console.warn/log/error）也需要保留，将其放入 effect.logMsg。
  </action>

  <acceptance_criteria>
  - `node -e "require('./services/task-manager');console.log('OK')"` 无报错
  - `grep -c "require.*task-state" services/task-manager.js` 输出 1
  - `wc -l services/task-manager.js`（或 Get-Content | Measure-Object 等价命令）显示行数 ≤ 350
  - checkHealth 函数在 task-manager.js 中的实现不超过 70 行
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: 最终验证 — 行数、测试、功能</name>
  <files>无（只运行验证命令）</files>

  <read_first>无需额外读取</read_first>

  <action>
  运行完整验证：
  1. `node -e "require('./services/task-state');console.log('task-state OK')"`
  2. `node -e "require('./services/task-manager');console.log('task-manager OK')"`
  3. 检查 task-manager.js 行数（必须 ≤ 350）
  4. `node scripts/check.js` — 必须通过
  5. `npm test` — 必须 26/26 通过
  </action>

  <acceptance_criteria>
  - task-manager.js 行数 ≤ 350
  - npm test 26/26 通过（或更多，不允许失败）
  - node scripts/check.js 无报错输出
  - `grep -c "^function buildCommand\|^function normalizeRecordLabel\|^async function syncDouyinHelper\|^async function syncAutoRecordingMediaFile" services/task-manager.js` 输出 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
1. `node -e "require('./services/task-state')"` — 无报错
2. `node -e "require('./services/task-manager')"` — 无报错
3. task-manager.js 行数 ≤ 350
4. `npm test` — 26/26 passed
5. `node scripts/check.js` — OK
</verification>

<success_criteria>
- [ ] services/task-state.js 存在，导出三个函数，全部为纯函数（无 db/ssh 调用）
- [ ] task-manager.js checkHealth 使用 task-state.js，缩减至约 60 行
- [ ] task-manager.js 总行数 ≤ 350
- [ ] npm test 26/26 PASS
- [ ] scripts/check.js 通过
</success_criteria>
