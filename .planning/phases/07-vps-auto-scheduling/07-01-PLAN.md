---
phase: 07-vps-auto-scheduling
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/vps-scheduler.js
  - __tests__/vps-scheduler.test.js
autonomous: true
requirements:
  - FEAT-01
must_haves:
  truths:
    - "selectBestVps(userId) 返回在线且 running_count 最少的 VPS 对象 { id, name }"
    - "无在线 VPS 时返回 null（不抛出异常）"
    - "已达 max_tasks_per_vps 上限的 VPS 不出现在候选列表"
    - "平局时按 vps.id 升序取第一个（确定性输出）"
    - "只查询当前用户的 VPS（多用户隔离）"
  artifacts:
    - path: "services/vps-scheduler.js"
      provides: "VPS 负载最低选择函数 selectBestVps(userId)"
      exports: ["selectBestVps"]
    - path: "__tests__/vps-scheduler.test.js"
      provides: "selectBestVps() 全分支单元测试"
      contains: "describe('selectBestVps'"
  key_links:
    - from: "services/vps-scheduler.js"
      to: "db.js"
      via: "require('../db')"
      pattern: "require\\('\\.\\./db'\\)"
    - from: "__tests__/vps-scheduler.test.js"
      to: "services/vps-scheduler.js"
      via: "require('../services/vps-scheduler')"
      pattern: "selectBestVps"
---

<objective>
创建 VPS 自动调度服务核心模块，并为其建立完整的单元测试。

Purpose: 调度器是 Phase 7 的核心能力——当任务 vps_id 为 NULL 时，能够从在线 VPS 中选出负载（运行中任务数）最低的节点。此模块必须先于 startTask() 修改完成，以便 Wave 2 直接引用。

Output:
- services/vps-scheduler.js — 纯查询逻辑，暴露 selectBestVps(userId)
- __tests__/vps-scheduler.test.js — Jest 单元测试，覆盖全部分支
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-vps-auto-scheduling/07-RESEARCH.md

<interfaces>
<!-- 来自 db.js，调度器需要的现有接口 -->

<!-- db (DatabaseSync 实例) — 通过 require('../db') 获取 -->
<!-- getSetting(key, userId) — 读取用户配置，返回字符串或 null -->
<!-- 例：getSetting('max_tasks_per_vps', userId) 返回 '5' 或用户设定值 -->

<!-- tasks 表活跃状态集合（与 checkHealth 监控口径一致）: -->
<!-- status IN ('running', 'source_retrying', 'target_lost', 'stalled', 'restarting') -->

<!-- vps 表在线判断: vps.status = 'online' -->
<!-- vps 表字段: id, user_id, name, status -->

<!-- 来自 utils/log-error.js -->
<!-- const { logError } = require('../utils/log-error') -->
<!-- logError(context, err) — 记录带上下文的结构化错误日志 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 vps-scheduler.js 调度服务</name>
  <files>services/vps-scheduler.js</files>
  <read_first>
    - f:\008 工具库\YouTube直播转推\restream-console\db.js （第 1-70 行，确认 db 导出方式和 getSetting 签名）
    - f:\008 工具库\YouTube直播转推\restream-console\services\task-manager.js （第 1-75 行，确认 active status 集合和 max_tasks_per_vps 读取方式）
    - f:\008 工具库\YouTube直播转推\restream-console\utils\log-error.js （确认 logError 导出名）
  </read_first>
  <behavior>
    - selectBestVps(userId) 传入 userId=1，VPS A 有 2 个 running 任务，VPS B 有 0 个任务，两者均 online → 返回 VPS B 的 { id, name }
    - selectBestVps(userId) 无在线 VPS（status != 'online'）→ 返回 null，不抛异常
    - selectBestVps(userId) VPS A running_count=2 >= max_tasks_per_vps=2 → 返回 null
    - selectBestVps(userId) VPS A（id=1）和 VPS B（id=2）均有 0 running → 返回 { id: 1, name: ... }（id 升序取第一）
    - selectBestVps(userId) userId=1 的 VPS 不会受 userId=2 的 VPS 影响
  </behavior>
  <action>
    创建 services/vps-scheduler.js，内容如下（严格按此实现）：

    ```javascript
    'use strict';
    const db = require('../db');
    const { getSetting } = require('../db');
    const { logError } = require('../utils/log-error');

    /**
     * 选出当前用户负载最低的在线 VPS。
     * 负载指标：tasks 表中 status IN ('running','source_retrying','target_lost','stalled','restarting') 的任务数。
     * 不考虑 CPU/内存（未持久化到 DB）。
     *
     * @param {number} userId
     * @returns {{ id: number, name: string } | null}
     *   成功返回 { id, name }；无可用 VPS 时返回 null（调用方负责抛出友好错误）
     */
    function selectBestVps(userId) {
      try {
        const maxPerVps = parseInt(getSetting('max_tasks_per_vps', userId) || '5');

        const row = db.prepare(`
          SELECT v.id, v.name,
            COUNT(t.id) AS running_count
          FROM vps v
          LEFT JOIN tasks t
            ON t.vps_id = v.id
           AND t.user_id = v.user_id
           AND t.status IN ('running', 'source_retrying', 'target_lost', 'stalled', 'restarting')
          WHERE v.user_id = ?
            AND v.status = 'online'
          GROUP BY v.id
          HAVING COUNT(t.id) < ?
          ORDER BY running_count ASC, v.id ASC
          LIMIT 1
        `).get(userId, maxPerVps);

        return row ? { id: row.id, name: row.name } : null;
      } catch (err) {
        logError('selectBestVps', err);
        return null;
      }
    }

    module.exports = { selectBestVps };
    ```

    注意：
    - HAVING COUNT(t.id) < maxPerVps 直接在 SQL 层过滤已达上限的 VPS，避免 Pitfall 1
    - try/catch 中记录日志后返回 null，不让调度失败崩溃 startTask()
    - 仅使用 db 和 getSetting（项目已有），无新依赖
  </action>
  <verify>
    <automated>node -e "const s = require('./services/vps-scheduler'); console.log(typeof s.selectBestVps === 'function' ? 'OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `services/vps-scheduler.js` 文件存在
    - 文件包含 `function selectBestVps(userId)` 函数定义
    - 文件末尾有 `module.exports = { selectBestVps }`
    - 文件包含 `HAVING COUNT(t.id) < ?` 子句（用于 max_tasks_per_vps 过滤）
    - 文件包含 `WHERE v.user_id = ?` 和 `AND v.status = 'online'`（用户隔离 + 在线过滤）
    - 文件包含 `ORDER BY running_count ASC, v.id ASC`（确定性排序）
    - 文件包含 `logError('selectBestVps', err)` 异常处理
    - `node -e "require('./services/vps-scheduler')"` 执行无报错
    - grep 验证：`grep -c "module.exports" services/vps-scheduler.js` 输出 `1`
  </acceptance_criteria>
  <done>services/vps-scheduler.js 可被 require，selectBestVps 是导出函数，无语法错误</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 编写 vps-scheduler 单元测试</name>
  <files>__tests__/vps-scheduler.test.js</files>
  <read_first>
    - f:\008 工具库\YouTube直播转推\restream-console\__tests__\task-manager.test.js （了解现有测试结构和 BASE_TASK 模式）
    - f:\008 工具库\YouTube直播转推\restream-console\db.js （第 1-80 行，了解 DatabaseSync 初始化方式，测试中需要创建内存 DB）
    - f:\008 工具库\YouTube直播转推\restream-console\services\vps-scheduler.js （刚创建的文件）
  </read_first>
  <behavior>
    - 测试套件独立运行，不依赖真实数据库文件（使用 jest.mock('../db') 模拟 db 和 getSetting）
    - 测试"有一个在线 VPS 有 0 任务"→ 返回该 VPS { id, name }
    - 测试"有两个在线 VPS，VPS-B 任务更少"→ 返回 VPS-B
    - 测试"无在线 VPS"→ 返回 null
    - 测试"唯一 VPS 已达 max_tasks_per_vps 上限"→ 返回 null
    - 测试"两个 VPS 任务数相同时按 id 升序"→ 返回 id 较小的 VPS
  </behavior>
  <action>
    创建 __tests__/vps-scheduler.test.js。

    关键设计：vps-scheduler.js 直接调用 `db.prepare(...).get(...)` 和 `getSetting()`。
    测试策略：使用 jest.mock('../db') 模拟整个 db 模块，让 db.prepare() 返回可控的 mock 数据。

    ```javascript
    'use strict';

    // Mock db 模块，在 require vps-scheduler 之前
    jest.mock('../db', () => {
      const mockGet = jest.fn();
      const mockPrepare = jest.fn(() => ({ get: mockGet }));
      return {
        prepare: mockPrepare,
        getSetting: jest.fn(),
        __mockGet: mockGet,
        __mockPrepare: mockPrepare,
      };
    });

    const db = require('../db');
    const { selectBestVps } = require('../services/vps-scheduler');

    beforeEach(() => {
      jest.clearAllMocks();
      // 默认 max_tasks_per_vps = 5
      db.getSetting.mockReturnValue('5');
    });

    describe('selectBestVps', () => {
      test('有一个在线 VPS 且无运行任务时，返回该 VPS', () => {
        db.__mockGet.mockReturnValue({ id: 1, name: 'VPS-A', running_count: 0 });
        const result = selectBestVps(1);
        expect(result).toEqual({ id: 1, name: 'VPS-A' });
      });

      test('有两个在线 VPS，返回任务数更少的那个', () => {
        // SQL 层已排序，mock 直接返回胜者
        db.__mockGet.mockReturnValue({ id: 2, name: 'VPS-B', running_count: 1 });
        const result = selectBestVps(1);
        expect(result).toEqual({ id: 2, name: 'VPS-B' });
      });

      test('无在线 VPS 时返回 null', () => {
        db.__mockGet.mockReturnValue(undefined);
        const result = selectBestVps(1);
        expect(result).toBeNull();
      });

      test('VPS 已达上限时返回 null', () => {
        db.getSetting.mockReturnValue('2');
        db.__mockGet.mockReturnValue(undefined); // HAVING 过滤后无结果
        const result = selectBestVps(1);
        expect(result).toBeNull();
      });

      test('两个 VPS 任务数相同时返回 id 较小的', () => {
        // SQL ORDER BY v.id ASC，mock 返回 id=1 的 VPS
        db.__mockGet.mockReturnValue({ id: 1, name: 'VPS-A', running_count: 0 });
        const result = selectBestVps(1);
        expect(result).toEqual({ id: 1, name: 'VPS-A' });
      });

      test('db 查询抛出异常时返回 null 不崩溃', () => {
        db.__mockPrepare.mockImplementation(() => {
          throw new Error('DB error');
        });
        const result = selectBestVps(1);
        expect(result).toBeNull();
      });

      test('调用 getSetting 时传入正确的 userId', () => {
        db.__mockGet.mockReturnValue(null);
        selectBestVps(42);
        // getSetting 第二个参数应为 userId
        expect(db.getSetting).toHaveBeenCalledWith('max_tasks_per_vps', 42);
      });
    });
    ```

    注意：mock 结构要与 vps-scheduler.js 中实际的调用方式完全对应：
    - `require('../db')` 返回 db 对象
    - `require('../db').getSetting` 是导出的 getSetting 函数
    - `db.prepare(sql).get(userId, maxPerVps)` 是查询链
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=vps-scheduler --forceExit</automated>
  </verify>
  <acceptance_criteria>
    - `__tests__/vps-scheduler.test.js` 文件存在
    - 文件包含 `describe('selectBestVps'` 测试套件
    - 文件包含至少 6 个 `test(` 或 `it(` 测试用例
    - `npm test -- --testPathPattern=vps-scheduler --forceExit` 输出包含 `Tests: X passed`，无 FAIL
    - 测试数量：`grep -c "  test(" __tests__/vps-scheduler.test.js` 输出 >= 6
    - `npm test` 全套测试全部通过（不破坏现有 task-manager 和 youtube-monitor 测试）
  </acceptance_criteria>
  <done>npm test -- --testPathPattern=vps-scheduler 全绿，npm test 全绿，无 FAIL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 描述 |
|------|------|
| userId 参数 | 来自 session，由调用方（startTask）已验证；调度器信任此值 |
| DB 查询结果 | 内部 DB，无外部输入，低风险 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | selectBestVps(userId) | mitigate | 查询严格过滤 `WHERE v.user_id = ?`，不同用户的 VPS 完全隔离 |
| T-07-02 | Denial of Service | db.prepare().get() | accept | DB 查询内部，无外部输入；try/catch 保护，异常不向上传播 |
</threat_model>

<verification>
```bash
# 1. 模块可加载
node -e "const s = require('./services/vps-scheduler'); console.log(Object.keys(s))"
# 期望输出: [ 'selectBestVps' ]

# 2. 单元测试全绿
npm test -- --testPathPattern=vps-scheduler --forceExit
# 期望: 6+ tests passed, 0 failed

# 3. 全套测试不回归
npm test
# 期望: 所有测试 passed
```
</verification>

<success_criteria>
1. services/vps-scheduler.js 存在，selectBestVps 函数可导入并调用
2. npm test -- --testPathPattern=vps-scheduler 全绿（≥6 tests）
3. npm test 全套（含 task-manager.test.js, youtube-monitor.test.js）全绿
4. 调度器 SQL 包含 max_tasks_per_vps HAVING 过滤、user_id 隔离、status='online' 条件
</success_criteria>

<output>
完成后创建 .planning/phases/07-vps-auto-scheduling/07-01-SUMMARY.md
</output>
