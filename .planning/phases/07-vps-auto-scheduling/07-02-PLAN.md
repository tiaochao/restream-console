---
phase: 07-vps-auto-scheduling
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - services/task-manager.js
autonomous: true
requirements:
  - FEAT-01
must_haves:
  truths:
    - "startTask() 传入 vps_id=NULL 的 taskId 时，调用 selectBestVps() 动态填充 vps_id"
    - "调度成功后立即执行 UPDATE tasks SET vps_id=? WHERE id=? 写回 DB（健康检测依赖此字段）"
    - "调度后写入 task_events 记录，reason 格式为 scheduler:VPS名(ID)"
    - "无可用 VPS 时抛出明确错误消息"无可用在线 VPS"，不崩溃"
    - "手动指定 vps_id 的任务完全不受影响，不调用调度器"
  artifacts:
    - path: "services/task-manager.js"
      provides: "修改后的 startTask()，在 vps_id 检查前插入调度逻辑"
      contains: "selectBestVps"
  key_links:
    - from: "services/task-manager.js startTask()"
      to: "services/vps-scheduler.js"
      via: "require('./vps-scheduler').selectBestVps"
      pattern: "selectBestVps"
    - from: "services/task-manager.js startTask()"
      to: "db tasks 表"
      via: "UPDATE tasks SET vps_id=?"
      pattern: "UPDATE tasks SET vps_id"
---

<objective>
修改 task-manager.js 的 startTask() 函数，在 vps_id 检查前插入 VPS 自动调度逻辑。

Purpose: 使 NULL vps_id 的任务能够自动获得 VPS 分配，完成 FEAT-01 的核心业务逻辑。调度器分配的 VPS ID 必须写回 DB，否则健康检测轮询（checkHealth）将因 `!task.vps_id` 跳过该任务，导致任务无人监控。

Output:
- services/task-manager.js 第 62 行前插入约 15 行调度逻辑
- 成功启动后追加 DB 写回 + writeEvent 记录
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-vps-auto-scheduling/07-RESEARCH.md
@.planning/phases/07-vps-auto-scheduling/07-01-SUMMARY.md

<interfaces>
<!-- 来自 services/vps-scheduler.js（Wave 1 新建）-->
<!-- const { selectBestVps } = require('./vps-scheduler'); -->
<!-- selectBestVps(userId: number): { id: number, name: string } | null -->
<!-- 返回 null 表示无可用在线 VPS，调用方负责抛出错误 -->

<!-- 来自 task-manager.js 现有代码（不变部分）-->
<!-- writeEvent(taskId, userId, fromStatus, toStatus, reason) — 已导入 -->
<!-- getSetting(key, userId) — 已导入 -->
<!-- db.prepare(sql).run(...) — 已导入 -->

<!-- startTask() 当前执行顺序（关键行号）：-->
<!-- 第54行: SELECT task + VPS -->
<!-- 第61行: if (!task) throw '任务不存在' -->
<!-- 第62行: if (!task.vps_id) throw '任务未绑定 VPS'  ← 修改此行前插入调度逻辑 -->
<!-- 第63行: if (running) throw '任务已在运行' -->
<!-- 第141行: UPDATE tasks SET status='running'... -->
<!-- 第148行: return pid -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 startTask() 中集成 VPS 自动调度</name>
  <files>services/task-manager.js</files>
  <read_first>
    - f:\008 工具库\YouTube直播转推\restream-console\services\task-manager.js （全文，343 行）
    - f:\008 工具库\YouTube直播转推\restream-console\services\vps-scheduler.js （确认函数签名和返回值）
  </read_first>
  <action>
    对 services/task-manager.js 进行以下精确修改：

    **修改 1：在第 62 行（`if (!task.vps_id) throw new Error('任务未绑定 VPS');`）之前插入调度块**

    将：
    ```javascript
      if (!task)          throw new Error('任务不存在');
      if (!task.vps_id)   throw new Error('任务未绑定 VPS');
      if (task.status === 'running') throw new Error('任务已在运行');
    ```

    改为：
    ```javascript
      if (!task) throw new Error('任务不存在');

      // [FEAT-01] vps_id 为 NULL 时调用调度器动态分配
      let scheduledVps = null;
      if (!task.vps_id) {
        const { selectBestVps } = require('./vps-scheduler');
        scheduledVps = selectBestVps(task.user_id);
        if (!scheduledVps) {
          throw new Error('无可用在线 VPS：所有节点均离线或已达任务上限，请在 VPS 管理页面检查连接状态');
        }
        task.vps_id = scheduledVps.id;
        task.vid    = scheduledVps.id;
        console.log(`[调度器] 任务 ${taskId} 自动分配到 VPS: ${scheduledVps.name}(${scheduledVps.id})`);
      }

      if (task.status === 'running') throw new Error('任务已在运行');
    ```

    **修改 2：在成功启动后（`return pid;` 之前）追加 DB 写回 + 事件记录**

    找到现有代码（约第 141-148 行）：
    ```javascript
      db.prepare(`
        UPDATE tasks
        SET status='running', remote_pid=?, log_file=?,
            started_at=datetime('now'), last_active_at=datetime('now'), stall_count=0, block_count=0
        WHERE id=?
      `).run(pid, logFile, taskId);

      return pid;
    ```

    改为：
    ```javascript
      db.prepare(`
        UPDATE tasks
        SET status='running', remote_pid=?, log_file=?,
            started_at=datetime('now'), last_active_at=datetime('now'), stall_count=0, block_count=0
        WHERE id=?
      `).run(pid, logFile, taskId);

      // [FEAT-01] 调度器分配后写回 vps_id（health check 依赖此字段，必须在 return 前完成）
      if (scheduledVps) {
        db.prepare('UPDATE tasks SET vps_id=? WHERE id=?').run(scheduledVps.id, taskId);
        writeEvent(taskId, task.user_id, task.status, 'running', `scheduler:${scheduledVps.name}(${scheduledVps.id})`);
      }

      return pid;
    ```

    **不修改任何其他代码**。手动指定 vps_id 的路径（scheduledVps 保持 null）完全不受影响。

    **风险提示（参见 RESEARCH.md Pitfall 2）**：调度后的 `UPDATE tasks SET vps_id=?` 必须在 `return pid` 之前执行，否则 checkHealth() 第 209 行的 `if (!task.remote_pid || !task.vps_id) return;` 会跳过健康检测，导致任务脱离监控。
  </action>
  <verify>
    <automated>node -e "const tm = require('./services/task-manager'); console.log(typeof tm.startTask === 'function' ? 'startTask OK' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `services/task-manager.js` 文件中包含 `scheduledVps = selectBestVps(task.user_id)` 调用
    - 文件中包含 `require('./vps-scheduler')` 引入
    - 文件中包含 `无可用在线 VPS：所有节点均离线` 错误消息（用于无 VPS 时的友好提示）
    - 文件中包含 `UPDATE tasks SET vps_id=? WHERE id=?` 语句（DB 写回）
    - 文件中包含 `scheduler:${scheduledVps.name}(${scheduledVps.id})` 事件 reason 格式
    - 文件中包含 `[调度器]` 日志前缀的 console.log
    - 原有 `throw new Error('任务未绑定 VPS')` 已被移除或替换（不再有该错误文本）
    - `node -e "require('./services/task-manager')"` 执行无报错
    - `npm test` 全套测试通过（含现有 task-manager.test.js）
    - grep 验证：`grep -c "scheduledVps" services/task-manager.js` 输出 >= 4（4 处使用）
  </acceptance_criteria>
  <done>task-manager.js 可加载，startTask 集成了调度逻辑，npm test 全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 描述 |
|------|------|
| startTask(taskId, userId) | userId 来自 session，已由路由层验证；taskId 来自路由参数 |
| selectBestVps 返回值 | 内部服务，返回 null 或 { id, name }，startTask 检查后使用 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | `task.vps_id` 内存赋值 | mitigate | DB 写回在 return 前完成（`UPDATE tasks SET vps_id=?`），不依赖内存值持久化 |
| T-07-04 | Denial of Service | 调度器连续调用 | accept | 调度器调用在 startTaskQueued 串行队列内，不存在并发竞态（RESEARCH Pitfall 3 已分析） |
| T-07-05 | Information Disclosure | 错误消息暴露 VPS 状态 | accept | 错误消息仅说明"无可用 VPS"，不暴露具体 VPS 信息；用户已是 VPS 所有者 |
</threat_model>

<verification>
```bash
# 1. 模块加载测试
node -e "require('./services/task-manager'); console.log('OK')"
# 期望: OK

# 2. 调度逻辑存在性检查
grep -n "scheduledVps" services/task-manager.js
# 期望: 找到 4+ 行（声明、调度调用、task.vps_id 赋值、DB 写回）

# 3. 旧错误消息已移除
grep "任务未绑定 VPS" services/task-manager.js
# 期望: 无输出（该错误已被替换）

# 4. 全套测试
npm test
# 期望: 全绿，无 FAIL
```
</verification>

<success_criteria>
1. services/task-manager.js 包含完整的自动调度逻辑（调度 → 内存赋值 → 日志 → 成功后 DB 写回 → writeEvent）
2. 原 `任务未绑定 VPS` 错误信息已替换为 `无可用在线 VPS` 友好提示
3. npm test 全套测试全绿（不回归现有测试）
4. 手动指定 vps_id 的任务路径：scheduledVps 保持 null，不触发 DB 写回
</success_criteria>

<output>
完成后创建 .planning/phases/07-vps-auto-scheduling/07-02-SUMMARY.md
</output>
