---
phase: 07-vps-auto-scheduling
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - views/tasks.ejs
  - views/stream-keys.ejs
  - views/task-detail.ejs
  - routes/tasks.js
autonomous: true
requirements:
  - FEAT-01
must_haves:
  truths:
    - "新建任务弹窗的 VPS 下拉框顶部显示"自动选择（负载最低）"选项，值为空字符串"
    - "任务列表中 vps_id=NULL 且 vps_name=NULL 的任务显示斜体"自动分配"文字"
    - "推流码编辑弹窗的默认 VPS 下拉框显示"自动（调度器分配）"作为空值选项"
    - "任务详情页基本信息中，若存在 scheduler: 开头的事件 reason，VPS 行显示"（调度器分配）"标注"
    - "详情页路由传入 schedulerAssigned 布尔值"
  artifacts:
    - path: "views/tasks.ejs"
      provides: "新建任务 VPS 下拉的"自动"选项 + 任务列表 VPS 列斜体显示"
      contains: "自动选择（负载最低）"
    - path: "views/stream-keys.ejs"
      provides: "推流码默认 VPS 下拉的"自动"选项文案"
      contains: "自动（调度器分配）"
    - path: "views/task-detail.ejs"
      provides: "基本信息 VPS 行的调度器标注"
      contains: "schedulerAssigned"
    - path: "routes/tasks.js"
      provides: "详情路由传入 schedulerAssigned 变量"
      contains: "schedulerAssigned"
  key_links:
    - from: "views/task-detail.ejs"
      to: "routes/tasks.js"
      via: "schedulerAssigned 模板变量"
      pattern: "schedulerAssigned"
    - from: "routes/tasks.js 详情路由"
      to: "task_events 表"
      via: "查询 reason LIKE 'scheduler:%'"
      pattern: "scheduler:%"
---

<objective>
实现 VPS 自动调度功能的前端 UI 改动，让用户在新建任务时能选择"自动"，并在任务列表和详情页看到系统分配的 VPS 信息。

Purpose: FEAT-01 的用户可见部分——用户需要知道任务是"自动分配"的，详情页需要显示"由调度器分配"来区别手动指定。所有改动为追加性修改，不删除任何现有功能。

Output:
- views/tasks.ejs — VPS 下拉新增"自动"选项 + 列表 VPS 列斜体标注
- views/stream-keys.ejs — "不绑定" 改为 "自动（调度器分配）"
- views/task-detail.ejs — 基本信息 VPS 行追加调度器标注
- routes/tasks.js — 详情路由传入 schedulerAssigned
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-vps-auto-scheduling/07-RESEARCH.md
@.planning/phases/07-vps-auto-scheduling/07-01-SUMMARY.md

<interfaces>
<!-- 来自 routes/tasks.js 详情路由（当前结构）-->
<!-- res.render('task-detail', { task, stats, recentEvents }) -->
<!-- task.vps_name — LEFT JOIN 提供，vps_id=NULL 时为 null -->
<!-- recentEvents — task_events 最近50条，每条含 { from_status, to_status, reason, created_at } -->
<!-- schedulerAssigned 需要新增：查询 recentEvents 中 reason LIKE 'scheduler:%' 是否存在 -->

<!-- 来自 views/tasks.ejs（当前结构）-->
<!-- 第216-222行：VPS select，当前只有"— 选择 VPS —"和 vpsList 循环 -->
<!-- 第133行：vps_name 列：<td><%= t.vps_name || '<span ...>未绑定</span>' %></td> -->

<!-- 来自 views/stream-keys.ejs（当前结构）-->
<!-- 第143行：<option value="">-- 不绑定 --</option> -->

<!-- 来自 views/task-detail.ejs（当前结构）-->
<!-- 第37行：<span style="color:var(--text-2);">VPS</span><span><%= task.vps_name || '--' %></span> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改任务列表和新建任务弹窗（tasks.ejs）</name>
  <files>views/tasks.ejs</files>
  <read_first>
    - f:\008 工具库\YouTube直播转推\restream-console\views\tasks.ejs （读取第 125-145 行查看 VPS 列渲染，读取第 210-225 行查看 VPS select 结构）
  </read_first>
  <action>
    对 views/tasks.ejs 进行两处精确修改：

    **修改 1：新建任务弹窗 VPS 下拉（约第 216-222 行）**

    找到：
    ```html
    <select name="vps_id" id="task-vps-select" onchange="loadMediaFiles(this.value)" class="form-select">
      <option value="">— 选择 VPS —</option>
      <% vpsList.forEach(function(v) { %>
        <option value="<%= v.id %>"><%= v.name %></option>
      <% }); %>
    </select>
    ```

    改为：
    ```html
    <select name="vps_id" id="task-vps-select" onchange="loadMediaFiles(this.value)" class="form-select">
      <option value="">自动选择（负载最低）</option>
      <% vpsList.forEach(function(v) { %>
        <option value="<%= v.id %>"><%= v.name %></option>
      <% }); %>
    </select>
    ```

    说明：value="" 保持不变，POST 时提交空字符串（等同于 NULL），路由层已有 `|| null` 处理。`applyStreamKey()` 函数在推流码 `default_vps_id=NULL` 时不修改此下拉，用户保持自动选项，无需修改 JS。

    **修改 2：任务列表 VPS 列（约第 133 行）**

    找到：
    ```html
    <td style="color:var(--text-2);"><%= t.vps_name || '<span style="color:var(--text-2)">未绑定</span>' %></td>
    ```

    改为（使用 EJS 条件块，避免 HTML 注入）：
    ```html
    <td style="color:var(--text-2);">
      <% if (t.vps_name) { %>
        <%= t.vps_name %>
      <% } else { %>
        <span style="color:var(--text-2);font-style:italic;">自动分配</span>
      <% } %>
    </td>
    ```

    说明：调度器分配后 vps_id 已写入 DB，JOIN 后 vps_name 有值，正常显示。任务处于 idle/waiting_live 且 vps_id=NULL 时，显示斜体"自动分配"提示用户此任务会自动调度。原来"未绑定"的场景（vps 被删除导致 vps_id=NULL）语义上与"自动分配"一致，无需区分。
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('views/tasks.ejs','utf8'); console.log(c.includes('自动选择（负载最低）') ? 'select-OK' : 'select-FAIL', c.includes('自动分配') ? 'list-OK' : 'list-FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "自动选择（负载最低）" views/tasks.ejs` 输出 `1`
    - `grep -c "自动分配" views/tasks.ejs` 输出 `1`
    - `grep -c "未绑定" views/tasks.ejs` 输出 `0`（原"未绑定"已替换为"自动分配"）
    - VPS select 第一个 option 的 value 仍为空字符串（`value=""`）：`grep 'option value=""' views/tasks.ejs` 包含"自动选择"
    - 文件中 `<select name="vps_id"` 标签仍存在，onchange 属性未被删除
  </acceptance_criteria>
  <done>tasks.ejs VPS 下拉第一个选项为"自动选择（负载最低）"，任务列表 VPS 列显示"自动分配"替代"未绑定"</done>
</task>

<task type="auto">
  <name>Task 2: 修改推流码默认 VPS 文案 + 任务详情调度器标注</name>
  <files>views/stream-keys.ejs, views/task-detail.ejs, routes/tasks.js</files>
  <read_first>
    - f:\008 工具库\YouTube直播转推\restream-console\views\stream-keys.ejs （第 138-150 行，查看默认 VPS select 结构）
    - f:\008 工具库\YouTube直播转推\restream-console\views\task-detail.ejs （第 32-45 行，查看基本信息 grid 结构）
    - f:\008 工具库\YouTube直播转推\restream-console\routes\tasks.js （第 141-180 行，查看详情路由完整代码）
  </read_first>
  <action>
    **文件 1：views/stream-keys.ejs（第 143 行）**

    找到：
    ```html
    <option value="">-- 不绑定 --</option>
    ```

    改为：
    ```html
    <option value="">自动（调度器分配）</option>
    ```

    说明：value="" 保持不变，推流码保存时 `default_vps_id = NULL`，启动任务时调度器自动选择。

    ---

    **文件 2：routes/tasks.js（详情路由，约第 165-179 行）**

    在 `res.render('task-detail', {...})` 调用之前，新增 schedulerAssigned 查询：

    找到：
    ```javascript
      const recentEvents = db.prepare(`
        SELECT id, from_status, to_status, reason, created_at
        FROM task_events
        WHERE task_id = ?
        ORDER BY created_at DESC
        LIMIT 50
      `).all(taskId);

      res.render('task-detail', {
        title: `任务详情 — ${task.name || task.id}`,
        currentPath: '/tasks',
        task,
        stats: statRow || { stall_count: 0, restart_count: 0, error_count: 0, recovered_count: 0 },
        recentEvents,
      });
    ```

    改为：
    ```javascript
      const recentEvents = db.prepare(`
        SELECT id, from_status, to_status, reason, created_at
        FROM task_events
        WHERE task_id = ?
        ORDER BY created_at DESC
        LIMIT 50
      `).all(taskId);

      // [FEAT-01] 检测该任务是否曾由调度器分配（用于详情页标注）
      const schedulerAssigned = recentEvents.some(ev => ev.reason && ev.reason.startsWith('scheduler:'));

      res.render('task-detail', {
        title: `任务详情 — ${task.name || task.id}`,
        currentPath: '/tasks',
        task,
        stats: statRow || { stall_count: 0, restart_count: 0, error_count: 0, recovered_count: 0 },
        recentEvents,
        schedulerAssigned,
      });
    ```

    ---

    **文件 3：views/task-detail.ejs（约第 37 行，基本信息 grid）**

    找到：
    ```html
          <span style="color:var(--text-2);">VPS</span><span><%= task.vps_name || '--' %></span>
    ```

    改为：
    ```html
          <span style="color:var(--text-2);">VPS</span>
          <span>
            <%= task.vps_name || '--' %>
            <% if (schedulerAssigned && task.vps_name) { %>
              <span style="font-size:11px;color:var(--text-2);margin-left:4px;">（调度器分配）</span>
            <% } %>
          </span>
    ```

    说明：
    - 只有当 task.vps_name 有值（VPS 已分配）且 schedulerAssigned 为 true 时，才显示标注
    - vps_name 为 null（VPS 被删除或尚未分配）时不显示标注，避免冗余
    - 标注为内联小字，不占额外行
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const sk=fs.readFileSync('views/stream-keys.ejs','utf8'); const td=fs.readFileSync('views/task-detail.ejs','utf8'); const rt=fs.readFileSync('routes/tasks.js','utf8'); console.log(sk.includes('自动（调度器分配）')?'sk-OK':'sk-FAIL', td.includes('schedulerAssigned')?'td-OK':'td-FAIL', rt.includes('schedulerAssigned')?'rt-OK':'rt-FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "自动（调度器分配）" views/stream-keys.ejs` 输出 `1`
    - `grep -c "-- 不绑定 --" views/stream-keys.ejs` 输出 `0`（原文案已替换）
    - `grep -c "schedulerAssigned" views/task-detail.ejs` 输出 >= `1`
    - `grep -c "schedulerAssigned" routes/tasks.js` 输出 >= `2`（定义 + 传参）
    - `grep "scheduler:" routes/tasks.js` 包含 `startsWith('scheduler:')` 查询逻辑
    - `node -e "require('./routes/tasks')"` 执行无报错（语法正确）
    - views/task-detail.ejs 中 `（调度器分配）` 标注在 `<% if (schedulerAssigned && task.vps_name) { %>` 条件内
    - views/stream-keys.ejs 中"自动"选项 value 仍为空字符串：`grep 'value=""' views/stream-keys.ejs` 包含"自动（调度器分配）"
  </acceptance_criteria>
  <done>三个文件改动完成：stream-keys.ejs 文案更新，routes/tasks.js 传入 schedulerAssigned，task-detail.ejs 显示调度器标注</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| 边界 | 描述 |
|------|------|
| task_events.reason 字段渲染 | reason 值在 task-detail.ejs 中已用 `<%= ev.reason %>` 输出，EJS 默认 HTML 转义，XSS 无风险 |
| vps_name 字段渲染 | 来自 DB，EJS 转义输出 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | XSS | task_events.reason 渲染 | accept | EJS `<%= %>` 自动 HTML 转义，scheduler:VPS名(ID) 格式为内部生成，无外部输入 |
| T-07-07 | Information Disclosure | schedulerAssigned 暴露调度历史 | accept | 仅在任务详情页（已认证用户）展示，不暴露其他用户数据 |
</threat_model>

<verification>
```bash
# 1. tasks.ejs 自动选项
grep "自动选择（负载最低）" views/tasks.ejs
# 期望: 找到一行

# 2. stream-keys.ejs 文案
grep "自动（调度器分配）" views/stream-keys.ejs
# 期望: 找到一行

# 3. 路由传参
grep "schedulerAssigned" routes/tasks.js
# 期望: 找到 2+ 行（定义 + res.render 传参）

# 4. 详情页标注
grep "schedulerAssigned" views/task-detail.ejs
# 期望: 找到 1+ 行

# 5. 路由无语法错误
node -e "require('./routes/tasks'); console.log('OK')"
# 期望: OK

# 6. 全套测试（路由改动不破坏现有测试）
npm test
# 期望: 全绿
```
</verification>

<success_criteria>
1. 新建任务弹窗 VPS 下拉第一选项显示"自动选择（负载最低）"，value=""
2. 任务列表中 vps_name 为空的任务显示斜体"自动分配"
3. 推流码编辑弹窗默认 VPS 显示"自动（调度器分配）"
4. 任务详情页对调度器分配的任务在 VPS 行显示"（调度器分配）"小字
5. npm test 全绿（所有 3 个文件改动不引入语法错误）
</success_criteria>

<output>
完成后创建 .planning/phases/07-vps-auto-scheduling/07-03-SUMMARY.md
</output>
