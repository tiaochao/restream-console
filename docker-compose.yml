services:
  restream-console:
    build: .
    container_name: restream-console
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
