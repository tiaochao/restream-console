<div class="page-header">
  <h2 class="page-title">日志</h2>
  <p class="page-subtitle">查看各任务的运行日志</p>
</div>

<div class="card section">
  <div class="card-header">
    <span class="card-title">有日志记录的任务</span>
  </div>
  <div style="overflow-x:auto;">
    <table class="data-table">
      <thead>
        <tr>
          <th>任务</th>
          <th>VPS</th>
          <th>状态</th>
          <th>日志文件</th>
          <th>操作</th>
        </tr>
      </thead>
      <tbody>
        <% if (tasks.length === 0) { %>
          <tr>
            <td colspan="5" style="text-align:center;padding:32px;color:var(--text-3);">暂无日志记录</td>
          </tr>
        <% } %>
        <% tasks.forEach(function(t) {
          const badgeMap = {
            running: 'badge-green',
            error:   'badge-red',
            stopped: 'badge-gray',
            idle:    'badge-gray'
          };
          const badge = badgeMap[t.status] || 'badge-gray';
        %>
          <tr>
            <td style="font-weight:500;color:var(--text-1);"><%= t.name || ('任务 #' + t.id) %></td>
            <td style="color:var(--text-2);"><%= t.vps_name || '--' %></td>
            <td><span class="badge <%= badge %>"><%= t.status %></span></td>
            <td class="monospace" style="font-size:12px;color:var(--text-3);max-width:260px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;">
              <%= t.log_file %>
            </td>
            <td>
              <a href="/logs/<%= t.id %>" class="btn btn-secondary btn-sm">查看日志</a>
            </td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  </div>
</div>
